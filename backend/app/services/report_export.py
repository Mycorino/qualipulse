"""Render a polished, self-contained HTML research report from a ProjectAnalysis.

The output is a single HTML document with all CSS inlined — no external
requests — designed to read beautifully on screen AND print cleanly to
A4/PDF via the browser's print dialog (a floating no-print toolbar offers
one-click "Save as PDF"). Localised EN/FR keyed off the project language.

Original data stays canonical: this module only *renders* what the analysis
pipeline produced — it never paraphrases quotes or re-scores anything.
"""

import html
import json
from datetime import datetime

_FREQ_LEVEL = {"all": 4, "most": 3, "some": 2, "few": 1}

_MONTHS_EN = ["January", "February", "March", "April", "May", "June", "July",
              "August", "September", "October", "November", "December"]
_MONTHS_FR = ["janvier", "février", "mars", "avril", "mai", "juin", "juillet",
              "août", "septembre", "octobre", "novembre", "décembre"]

_STRINGS = {
    "en": {
        "doc_type": "Research findings report",
        "generated": "Generated",
        "interviews": "completed interviews",
        "confidence": "Confidence",
        "confidence_low": "Low", "confidence_medium": "Medium", "confidence_high": "High",
        "version": "Analysis v{v}",
        "refined": "Researcher-refined",
        "ai_discovery": "AI discovery",
        "segment_filter": "Segment filter",
        "exec_summary": "Executive summary",
        "at_a_glance": "At a glance",
        "stat_participants": "Interviews analysed",
        "stat_themes": "Key themes",
        "stat_jtbd": "Jobs to be done",
        "stat_tensions": "Tensions",
        "stat_recos": "Recommendations",
        "study_design": "Study design",
        "objective": "Research objective",
        "decision": "Decision this informs",
        "target": "Target participants",
        "duration": "Interview length",
        "duration_value": "{m} minutes (target)",
        "field_dates": "Fieldwork window",
        "sample_profile": "Sample profile",
        "sample_profile_value": "{n} participants · {professions} professions · {countries} countries",
        "analysis_lineage": "Analysis version",
        "lineage_refined": "v{v} — refined by the researcher from v{p}",
        "lineage_ai": "v{v} — AI discovery synthesis",
        "themes_title": "Key themes",
        "themes_sub": "Every theme is supported by verbatim quotes from at least two participants. Frequency reflects how widely it appeared across the sample.",
        "freq_all": "Heard from all participants",
        "freq_most": "Heard from most participants",
        "freq_some": "Heard from some participants",
        "freq_few": "Heard from a few participants",
        "disconfirming": "Disconfirming evidence",
        "researcher_note": "Researcher note",
        "annot_confirmed": "Confirmed by researcher",
        "annot_disputed": "Disputed by researcher",
        "annot_needs_evidence": "Flagged: needs more evidence",
        "quote_prompt": "In response to",
        "quote_unverified": "⚠ Not found verbatim in the transcript — verify before citing.",
        "evidence_map": "Evidence map",
        "evidence_map_sub": "Which participants support each theme. A filled dot means the participant is quoted in that theme.",
        "theme_col": "Theme",
        "jtbd_title": "Jobs to be done",
        "jtbd_sub": "Underlying motivations expressed across interviews, framed as jobs.",
        "insight": "Insight",
        "tensions_title": "Tensions & contradictions",
        "tensions_sub": "Forced choices and contradictions in the data — treat these as open questions, not conclusions.",
        "recos_title": "Recommendations",
        "recos_sub": "Decision-oriented next steps. Each one states what evidence would prove it wrong.",
        "appendix": "Appendix — participants",
        "col_id": "ID", "col_name": "Name", "col_profession": "Profession",
        "col_age": "Age", "col_country": "Country", "col_quality": "Response quality",
        "col_completed": "Completed",
        "quality_low": "Low", "quality_fair": "Fair", "quality_good": "Good", "quality_strong": "Strong",
        "confidence_note": "How to read this confidence level",
        "contract_title": "Methodology contract",
        "contract_items": [
            "Every theme cites verbatim quotes from at least two distinct participants; single-voice signals are reported as tensions, not themes.",
            "Quotes are verified against the source transcripts; anything that cannot be matched verbatim is flagged in the document.",
            "Confidence follows sample size: N≤5 is directional, N=6–9 suggestive; high confidence requires N≥10 with thematic saturation.",
            "Analysis generated by AI (Claude) from the full interview transcripts, reviewed and refinable by the researcher — annotations feed the next version.",
        ],
        "footer": "Generated with QualiPulse — AI-moderated voice interviews, analysed with full quote traceability.",
        # recommendation activation + priority matrix + 30-60-90 plan
        "reco_owner": "Owner", "reco_horizon": "Horizon",
        "reco_kpi": "Success metric", "reco_falsifier": "Would be wrong if",
        "impact_label": "Impact", "effort_label": "Effort",
        "level_low": "Low", "level_medium": "Medium", "level_high": "High",
        "horizon_now": "Now", "horizon_30d": "30 days",
        "horizon_60_90d": "60–90 days", "horizon_later": "Later",
        "plan_title": "Activation plan — first 90 days",
        "plan_sub": "Recommendations sequenced by when to act. Items marked “later” need more evidence first.",
        "matrix_title": "Priority matrix",
        "matrix_sub": "Impact against effort. Numbers match the recommendations above.",
        "matrix_x": "Effort →", "matrix_y": "Impact →",
        "quad_quickwins": "Quick wins", "quad_bigbets": "Big bets",
        "quad_fillins": "Fill-ins", "quad_moneypits": "Money pits",
        # personas
        "personas_title": "Personas",
        "personas_sub": "Archetypes synthesised from real participants; each names the interviews it is built from.",
        "persona_built_from": "Built from", "persona_goals": "Goals",
        "persona_frustrations": "Frustrations", "persona_behaviours": "Behaviours",
        "persona_job": "Primary job",
        # journey / emotion map
        "journey_title": "Experience journey",
        "journey_sub": "How the experience unfolds, stage by stage, with the emotional arc drawn from what participants said.",
        "journey_emotion": "Emotional arc", "journey_stage": "Stage",
        "journey_goal": "Goal", "journey_pain": "Friction",
        "journey_opportunity": "Opportunity", "journey_quote_col": "Voice",
        "print_btn": "Print / Save as PDF",
        "anonymous": "Participant",
    },
    "fr": {
        "doc_type": "Rapport de résultats de recherche",
        "generated": "Généré le",
        "interviews": "entretiens terminés",
        "confidence": "Confiance",
        "confidence_low": "Faible", "confidence_medium": "Moyenne", "confidence_high": "Élevée",
        "version": "Analyse v{v}",
        "refined": "Affinée par le chercheur",
        "ai_discovery": "Découverte IA",
        "segment_filter": "Filtre de segment",
        "exec_summary": "Synthèse",
        "at_a_glance": "En un coup d'œil",
        "stat_participants": "Entretiens analysés",
        "stat_themes": "Thèmes clés",
        "stat_jtbd": "Jobs to be done",
        "stat_tensions": "Tensions",
        "stat_recos": "Recommandations",
        "study_design": "Protocole de l'étude",
        "objective": "Objectif de recherche",
        "decision": "Décision éclairée",
        "target": "Participants cibles",
        "duration": "Durée d'entretien",
        "duration_value": "{m} minutes (cible)",
        "field_dates": "Période de terrain",
        "sample_profile": "Profil de l'échantillon",
        "sample_profile_value": "{n} participants · {professions} professions · {countries} pays",
        "analysis_lineage": "Version d'analyse",
        "lineage_refined": "v{v} — affinée par le chercheur à partir de la v{p}",
        "lineage_ai": "v{v} — synthèse de découverte IA",
        "themes_title": "Thèmes clés",
        "themes_sub": "Chaque thème s'appuie sur des citations verbatim d'au moins deux participants. La fréquence reflète sa présence dans l'échantillon.",
        "freq_all": "Entendu chez tous les participants",
        "freq_most": "Entendu chez la plupart des participants",
        "freq_some": "Entendu chez certains participants",
        "freq_few": "Entendu chez quelques participants",
        "disconfirming": "Preuve contradictoire",
        "researcher_note": "Note du chercheur",
        "annot_confirmed": "Confirmé par le chercheur",
        "annot_disputed": "Contesté par le chercheur",
        "annot_needs_evidence": "Signalé : preuves insuffisantes",
        "quote_prompt": "En réponse à",
        "quote_unverified": "⚠ Introuvable telle quelle dans le verbatim — à vérifier avant citation.",
        "evidence_map": "Carte des preuves",
        "evidence_map_sub": "Quels participants soutiennent chaque thème. Un point plein signifie que le participant est cité dans ce thème.",
        "theme_col": "Thème",
        "jtbd_title": "Jobs to be done",
        "jtbd_sub": "Motivations sous-jacentes exprimées au fil des entretiens, formulées comme des « jobs ».",
        "insight": "Enseignement",
        "tensions_title": "Tensions & contradictions",
        "tensions_sub": "Choix forcés et contradictions dans les données — à traiter comme des questions ouvertes, pas des conclusions.",
        "recos_title": "Recommandations",
        "recos_sub": "Prochaines étapes orientées décision. Chacune précise ce qui la réfuterait.",
        "appendix": "Annexe — participants",
        "col_id": "ID", "col_name": "Nom", "col_profession": "Profession",
        "col_age": "Âge", "col_country": "Pays", "col_quality": "Qualité des réponses",
        "col_completed": "Terminé le",
        "quality_low": "Faible", "quality_fair": "Passable", "quality_good": "Bonne", "quality_strong": "Solide",
        "confidence_note": "Comment lire ce niveau de confiance",
        "contract_title": "Contrat méthodologique",
        "contract_items": [
            "Chaque thème cite des verbatims d'au moins deux participants distincts ; les signaux à voix unique sont rapportés comme tensions, pas comme thèmes.",
            "Les citations sont vérifiées contre les transcripts sources ; toute citation introuvable telle quelle est signalée dans le document.",
            "La confiance suit la taille d'échantillon : N≤5 est indicatif, N=6–9 suggestif ; une confiance élevée exige N≥10 avec saturation thématique.",
            "Analyse générée par IA (Claude) à partir des transcripts complets, relue et affinable par le chercheur — les annotations nourrissent la version suivante.",
        ],
        "footer": "Généré avec QualiPulse — entretiens vocaux menés par IA, analysés avec traçabilité complète des citations.",
        # recommandations activables + matrice de priorisation + plan 30-60-90
        "reco_owner": "Responsable", "reco_horizon": "Horizon",
        "reco_kpi": "Indicateur de succès", "reco_falsifier": "Serait réfuté si",
        "impact_label": "Impact", "effort_label": "Effort",
        "level_low": "Faible", "level_medium": "Moyen", "level_high": "Élevé",
        "horizon_now": "Maintenant", "horizon_30d": "30 jours",
        "horizon_60_90d": "60–90 jours", "horizon_later": "Plus tard",
        "plan_title": "Plan d'activation — 90 premiers jours",
        "plan_sub": "Recommandations séquencées selon le moment d'agir. Les éléments « plus tard » demandent d'abord plus de preuves.",
        "matrix_title": "Matrice de priorisation",
        "matrix_sub": "Impact selon l'effort. Les numéros renvoient aux recommandations ci-dessus.",
        "matrix_x": "Effort →", "matrix_y": "Impact →",
        "quad_quickwins": "Gains rapides", "quad_bigbets": "Gros paris",
        "quad_fillins": "À-côtés", "quad_moneypits": "Gouffres",
        # personas
        "personas_title": "Personas",
        "personas_sub": "Archétypes synthétisés à partir de participants réels ; chacun nomme les entretiens dont il est issu.",
        "persona_built_from": "Construit à partir de", "persona_goals": "Objectifs",
        "persona_frustrations": "Frustrations", "persona_behaviours": "Comportements",
        "persona_job": "Job principal",
        # parcours / courbe émotionnelle
        "journey_title": "Parcours d'expérience",
        "journey_sub": "Le déroulé de l'expérience, étape par étape, avec la courbe émotionnelle issue des propos des participants.",
        "journey_emotion": "Courbe émotionnelle", "journey_stage": "Étape",
        "journey_goal": "Objectif", "journey_pain": "Friction",
        "journey_opportunity": "Opportunité", "journey_quote_col": "Verbatim",
        "print_btn": "Imprimer / Enregistrer en PDF",
        "anonymous": "Participant",
    },
}


def _esc(value) -> str:
    return html.escape(str(value)) if value else ""


def _fmt_date(dt: datetime | None, lang: str) -> str:
    if not dt:
        return "—"
    months = _MONTHS_FR if lang == "fr" else _MONTHS_EN
    return f"{dt.day} {months[dt.month - 1]} {dt.year}"


# ── Report colour identities ───────────────────────────────────────────
# Three document families, three inks — a reader can tell which kind of
# evidence they are holding from across the room:
#   qual  — forest green (interview findings: the field notebook)
#   quant — ink blue     (survey results: the statistical tables)
#   mixed — bordeaux     (quantified themes + decision memo: the board document)
# The decision memo is the flagship: same bordeaux ink, boldest layout.
_PALETTES = {
    "qual":  {"accent": "#1d5c3f", "tint": "#eef4ef", "deep": "#10382a", "hover": "#16452f"},
    "quant": {"accent": "#1e4a73", "tint": "#edf2f8", "deep": "#132f4b", "hover": "#173a5b"},
    "mixed": {"accent": "#7c2434", "tint": "#f8eef0", "deep": "#4d1420", "hover": "#651c2a"},
}


def _palette_css(kind: str) -> str:
    """CSS variable override appended after a document's stylesheet.

    Later rules win, so this re-inks --accent / --accent-tint (and the
    hardcoded toolbar hover) without touching the shared layout CSS.
    """
    p = _PALETTES.get(kind, _PALETTES["qual"])
    return (
        "\n:root { --accent: %(accent)s; --accent-tint: %(tint)s; --accent-deep: %(deep)s; }\n"
        ".toolbar button:hover { background: %(hover)s; }\n" % p
    )


def _accent(kind: str) -> str:
    return _PALETTES.get(kind, _PALETTES["qual"])["accent"]


def _freq_meter(frequency: str, L: dict) -> str:
    level = _FREQ_LEVEL.get((frequency or "").lower(), 0)
    label = L.get(f"freq_{(frequency or '').lower()}", _esc(frequency))
    dots = "".join(
        f'<span class="fdot{" fdot--on" if i < level else ""}"></span>'
        for i in range(4)
    )
    return f'<span class="freq" title="{_esc(label)}">{dots}<span class="freq__label">{_esc(label)}</span></span>'


def _confidence_badge(confidence: str, L: dict) -> str:
    c = (confidence or "").lower()
    label = L.get(f"confidence_{c}", _esc(confidence))
    sep = "&nbsp;:" if L is _STRINGS["fr"] else ":"
    return '<span class="chip chip--conf chip--conf-{}">{}{} {}</span>'.format(
        _esc(c), L["confidence"], sep, label
    )



# ── Shared document design (evergreen serif) ──────────────────────────
# One visual language across all three exports (qualitative findings,
# quantified themes, decision memo) so the evidence chain reads as one
# system. Tokens mirror _STUDY_CSS below — keep them in sync.

_BASE_DOC_CSS = """
:root {
  --paper: #fcfcfa; --ink: #17201b; --ink-2: #4c5852; --ink-3: #7a847e;
  --rule: #dfe5e0; --rule-strong: #17201b;
  --accent: #1d5c3f; --accent-tint: #eef4ef;
  --copper: #a2541f; --copper-tint: #f7eee5;
  --amber: #8a5a17; --amber-tint: #f6efdf;
  --gap-bg: #edefed;
  --serif: "Iowan Old Style", "Palatino Linotype", Palatino, "Charter", Georgia, serif;
  --sans: "Avenir Next", "Segoe UI", -apple-system, "Helvetica Neue", sans-serif;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
html { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
body { font-family: var(--sans); color: var(--ink); background: var(--paper);
  font-size: 15px; line-height: 1.6; }
.sheet { max-width: 780px; margin: 0 auto; padding: 56px 40px 64px; }
@media (max-width: 720px) { .sheet { padding: 32px 20px; } }

.cover { border-bottom: 2px solid var(--rule-strong); padding-bottom: 24px; margin-bottom: 10px; }
.cover__brand { display: flex; align-items: baseline; gap: 12px; }
.cover__logo { font-weight: 700; font-size: 1rem; color: var(--accent); letter-spacing: -0.01em; }
.cover__doctype { font-size: 11px; font-weight: 600; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--accent); }
.cover__title { font-family: var(--serif); font-size: 2.1rem; font-weight: 600;
  line-height: 1.22; letter-spacing: -0.01em; margin: 12px 0 14px; text-wrap: balance; }
.cover__meta { display: flex; flex-wrap: wrap; gap: 6px 20px; align-items: baseline;
  font-size: 12.5px; color: var(--ink-2); }
.cover__meta .chip { background: none; border: 0; padding: 0; font-size: 12.5px;
  font-weight: 400; letter-spacing: 0; text-transform: none; color: var(--ink-2); }
.cover__meta .chip--strong { font-weight: 600; color: var(--ink); }

.chip { display: inline-block; font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; padding: 2px 8px; border-radius: 3px; white-space: nowrap;
  background: var(--gap-bg); color: var(--ink-2); }
.cover__meta .chip--conf-high, .cover__meta .chip--conf-medium, .cover__meta .chip--conf-low,
.cover__meta .chip--filter { font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; padding: 2px 8px; border-radius: 3px; }
.chip--conf-high { background: var(--accent-tint); color: var(--accent); }
.chip--conf-medium { background: var(--amber-tint); color: var(--amber); }
.chip--conf-low { background: var(--copper-tint); color: var(--copper); }
.chip--filter { background: var(--amber-tint); color: var(--amber); }

.eyebrow { font-size: 12px; font-weight: 700; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--ink); border-bottom: 1px solid var(--rule);
  padding-bottom: 8px; margin-bottom: 18px; }
section { margin-top: 44px; margin-bottom: 0; }
.section-title { font-size: 12px; font-weight: 700; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--ink); border-bottom: 1px solid var(--rule);
  padding-bottom: 8px; margin-bottom: 8px; }
.section-sub { font-size: 12.5px; color: var(--ink-3); margin-bottom: 22px; max-width: 70ch; }

.rationale { margin-top: 14px; font-size: 13px; color: var(--ink-2);
  background: #f4f6f4; padding: 12px 16px; max-width: 680px; }

.stats { display: grid; grid-template-columns: repeat(5, 1fr); gap: 22px; }
@media (max-width: 720px) { .stats { grid-template-columns: repeat(2, 1fr); } }
.stat { text-align: left; }
.stat__num { font-family: var(--serif); font-size: 1.9rem; font-weight: 600;
  line-height: 1.1; font-variant-numeric: tabular-nums; }
.stat__label { font-size: 11.5px; color: var(--ink-2); margin-top: 4px; line-height: 1.4; }

.quote { border-left: 2px solid var(--rule); padding: 2px 0 2px 14px; margin: 12px 0; }
.quote p { font-family: var(--serif); font-size: 1rem; font-style: italic;
  line-height: 1.5; color: var(--ink); }
.quote footer { font-size: 12px; color: var(--ink-2); margin-top: 6px; }
.quote__prompt { font-size: 11.5px; color: var(--ink-3); margin-top: 3px; }
.quote__unverified { font-size: 11.5px; color: var(--copper); margin-top: 4px; font-weight: 600; }

.contract { background: #f4f6f4; padding: 16px 20px; font-size: 13px; margin-top: 22px; max-width: 680px; }
.contract h4 { font-size: 11px; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--ink-2); margin-bottom: 8px; }
.contract ul { margin: 0; padding-left: 18px; }
.contract li { margin-bottom: 5px; }

.recos { list-style: none; }
.reco { display: flex; gap: 14px; align-items: baseline;
  border-bottom: 1px solid var(--rule); padding: 12px 0; }
.reco__num { flex: 0 0 auto; font-family: var(--serif); font-size: 1.05rem;
  color: var(--accent); font-variant-numeric: tabular-nums; }
.reco p { font-size: 14px; }

.doc-footer { margin-top: 52px; border-top: 1px solid var(--rule); padding-top: 14px;
  font-size: 12px; color: var(--ink-3); display: flex; justify-content: space-between;
  flex-wrap: wrap; gap: 8px; }
.doc-footer strong { color: var(--accent); }
.toolbar { position: fixed; top: 18px; right: 18px; z-index: 10; }
.toolbar button { font: inherit; font-size: 0.82rem; font-weight: 600; color: #fff;
  background: var(--accent); border: 0; border-radius: 999px; padding: 10px 18px;
  cursor: pointer; box-shadow: 0 4px 16px rgba(23,32,27,0.18); }
.toolbar button:hover { background: #16452f; }
.avoid-break { break-inside: avoid; page-break-inside: avoid; }
@media (max-width: 600px) {
  .toolbar { position: static; display: flex; justify-content: flex-end; padding: 16px 20px 0; }
}
@media print {
  body { background: #fff; font-size: 12.5px; }
  .sheet { max-width: none; padding: 0; }
  .toolbar { display: none; }
  section { margin-top: 30px; }
  .page-break { break-before: page; page-break-before: always; }
  @page { size: A4; margin: 16mm 14mm; }
}
"""

_ANALYSIS_CSS = _BASE_DOC_CSS + """
.summary { background: var(--accent-tint); border-left: 3px solid var(--accent);
  padding: 18px 22px; max-width: 680px; }
.summary .eyebrow { border-bottom: 0; padding-bottom: 0; margin-bottom: 8px; color: var(--accent); }
.summary__text { font-family: var(--serif); font-size: 1.3rem; line-height: 1.45; font-weight: 500; }

.design { border-top: 1px solid var(--rule); }
.design__row { display: grid; grid-template-columns: 220px 1fr; border-bottom: 1px solid var(--rule); }
.design__row dt { padding: 10px 16px 10px 0; font-size: 11px; font-weight: 700;
  color: var(--ink-2); text-transform: uppercase; letter-spacing: 0.08em; }
.design__row dd { padding: 10px 0; font-size: 13.5px; }
@media (max-width: 720px) { .design__row { grid-template-columns: 1fr; } .design__row dd { padding-top: 0; } }

.theme { border-top: 1px solid var(--rule); padding: 22px 0 26px; }
.theme:first-of-type { border-top: 0; padding-top: 6px; }
.theme__head { display: flex; align-items: baseline; gap: 12px; flex-wrap: wrap; margin-bottom: 8px; }
.theme__num { font-family: var(--serif); font-size: 1rem; color: var(--accent);
  font-variant-numeric: tabular-nums; }
.theme__title { font-family: var(--serif); font-size: 1.35rem; font-weight: 600;
  line-height: 1.3; letter-spacing: -0.005em; flex: 1; text-wrap: balance; }
.theme__summary { font-size: 14px; color: var(--ink-2); margin-bottom: 12px; max-width: 74ch; }

.freq { display: inline-flex; align-items: center; gap: 4px; }
.fdot { width: 7px; height: 7px; border-radius: 50%; background: var(--rule); display: inline-block; }
.fdot--on { background: var(--accent); }
.freq__label { font-size: 11.5px; color: var(--ink-3); margin-left: 4px; }

.callout { padding: 12px 16px; margin-top: 14px; font-size: 13px; max-width: 680px; }
.callout__title { font-size: 10.5px; font-weight: 700; text-transform: uppercase;
  letter-spacing: 0.08em; margin-bottom: 4px; }
.callout--warn { background: var(--copper-tint); border-left: 3px solid var(--copper); }
.callout--warn .callout__title { color: var(--copper); }
.callout--note { background: var(--accent-tint); border-left: 3px solid var(--accent); }
.callout--note .callout__title { color: var(--accent); }

.annot { display: inline-block; font-size: 11px; font-weight: 600; padding: 3px 10px;
  border-radius: 3px; margin-bottom: 10px; }
.annot--confirmed { background: var(--accent-tint); color: var(--accent); }
.annot--disputed { background: var(--copper-tint); color: var(--copper); }
.annot--needs_evidence { background: var(--amber-tint); color: var(--amber); }

.ev { width: 100%; border-collapse: collapse; font-size: 12.5px; }
.ev th { font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong); padding: 0 8px 6px; text-align: center; }
.ev td { border-bottom: 1px solid var(--rule); padding: 8px; text-align: center; }
.ev__theme { text-align: left !important; font-weight: 600; max-width: 300px; padding-left: 0 !important; }
.dot { display: inline-block; width: 10px; height: 10px; border-radius: 50%; }
.dot--on { background: var(--accent); }
.dot--off { background: var(--rule); width: 6px; height: 6px; }
.ev__legend { font-size: 11.5px; color: var(--ink-3); margin-top: 10px; }

.jtbd-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 0 32px; }
@media (max-width: 720px) { .jtbd-grid { grid-template-columns: 1fr; } }
.jtbd { border-top: 1px solid var(--rule); padding: 16px 0 18px; }
.jtbd__job { font-family: var(--serif); font-weight: 600; font-size: 1.02rem;
  line-height: 1.4; margin-bottom: 8px; }
.jtbd__insight { font-size: 13px; color: var(--ink-2); margin-bottom: 10px; }

.tension { border-left: 3px solid var(--copper); padding: 2px 0 2px 16px; margin-bottom: 18px; }
.tension__label { font-family: var(--serif); font-size: 1.02rem; font-weight: 600; margin-bottom: 4px; }
.tension p { font-size: 13.5px; color: var(--ink-2); max-width: 72ch; }

.roster { width: 100%; border-collapse: collapse; font-size: 12.5px; }
.roster th { text-align: left; font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong);
  padding: 0 14px 6px 0; }
.roster td { border-bottom: 1px solid var(--rule); padding: 8px 14px 8px 0; vertical-align: top; }
.q { font-size: 11px; font-weight: 600; padding: 2px 8px; border-radius: 3px; }
.q--strong, .q--good { background: var(--accent-tint); color: var(--accent); }
.q--fair { background: var(--amber-tint); color: var(--amber); }
.q--low { background: var(--copper-tint); color: var(--copper); }
.q--none { background: var(--gap-bg); color: var(--ink-3); }

/* rich recommendations (object shape) */
.reco--rich { align-items: flex-start; }
.reco__body { flex: 1; }
.reco .reco__action { font-family: var(--serif); font-size: 1.06rem; font-weight: 600; line-height: 1.35; }
.reco .reco__rationale { font-size: 13px; color: var(--ink-2); margin-top: 4px; }
.reco__meta { display: flex; flex-wrap: wrap; gap: 6px 10px; margin: 10px 0 6px; }
.reco__tag { font-size: 11px; background: var(--gap-bg); color: var(--ink-2); padding: 3px 9px; border-radius: 3px; }
.reco__tag b { font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase; font-size: 9.5px;
  color: var(--ink-3); margin-right: 5px; }
.reco__tag--impact { background: var(--accent-tint); color: var(--accent); }
.reco__tag--impact b { color: var(--accent); }
.reco__tag--effort { background: var(--amber-tint); color: var(--amber); }
.reco__tag--effort b { color: var(--amber); }
.reco .reco__kpi, .reco .reco__falsifier { font-size: 12.5px; color: var(--ink-2); margin-top: 5px; }
.reco__kpi b, .reco__falsifier b { font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase;
  font-size: 9.5px; color: var(--ink-3); margin-right: 6px; }

/* 30-60-90 activation plan */
.plan { display: grid; grid-template-columns: repeat(3, 1fr); gap: 20px; }
@media (max-width: 720px) { .plan { grid-template-columns: 1fr; } }
.plan__col { border-top: 2px solid var(--accent); padding-top: 10px; }
.plan__when { font-size: 11px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--accent); margin-bottom: 10px; }
.plan__item { font-size: 12.5px; padding: 8px 0; border-bottom: 1px solid var(--rule); }
.plan__item:last-child { border-bottom: 0; }
.plan__owner { display: block; font-size: 10.5px; color: var(--ink-3); margin-top: 3px; }
.plan__empty { font-size: 12px; color: var(--ink-3); font-style: italic; }

/* personas */
.persona-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 22px 30px; }
@media (max-width: 720px) { .persona-grid { grid-template-columns: 1fr; } }
.persona { border-top: 2px solid var(--accent); padding-top: 12px; }
.persona__name { font-family: var(--serif); font-size: 1.22rem; font-weight: 600; line-height: 1.25; }
.persona__seg { font-size: 11px; color: var(--ink-3); margin-top: 2px; }
.persona__from { margin: 8px 0; display: flex; align-items: baseline; flex-wrap: wrap; gap: 4px; }
.persona__from-label { font-size: 10px; text-transform: uppercase; letter-spacing: 0.06em; color: var(--ink-3); }
.persona__pill { display: inline-block; font-size: 10.5px; font-weight: 700; background: var(--accent-tint);
  color: var(--accent); padding: 2px 8px; border-radius: 3px; }
.persona__oneliner { font-size: 13px; color: var(--ink-2); margin: 6px 0 10px; }
.persona__list { margin: 8px 0; }
.persona__list h5 { font-size: 9.5px; font-weight: 700; text-transform: uppercase; letter-spacing: 0.06em;
  color: var(--ink-3); margin-bottom: 3px; }
.persona__list ul { margin: 0; padding-left: 16px; font-size: 12.5px; color: var(--ink-2); }
.persona__list li { margin-bottom: 2px; }
.persona__job { font-size: 12.5px; margin: 8px 0; }
.persona__job b { font-weight: 700; letter-spacing: 0.04em; text-transform: uppercase; font-size: 9.5px;
  color: var(--ink-3); margin-right: 5px; }

/* journey / emotion map */
.journey__label { font-family: var(--serif); font-size: 1rem; color: var(--ink-2); margin: 2px 0 12px; }
.journey__table { width: 100%; border-collapse: collapse; font-size: 12px; margin-top: 16px; }
.journey__table th { text-align: left; font-size: 10px; font-weight: 700; text-transform: uppercase;
  letter-spacing: 0.06em; color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong);
  padding: 0 10px 6px 0; }
.journey__table td { border-bottom: 1px solid var(--rule); padding: 8px 10px 8px 0; vertical-align: top; }
.journey__stage { font-weight: 600; }
.journey__emo { font-variant-numeric: tabular-nums; font-weight: 600; text-align: center; }
.journey__pain { color: var(--copper); }
"""


# ── Reusable qualitative report sections ────────────────────────────────────
# Extracted from render_analysis_report_html so the Decision report can compose
# the SAME qual exhibits (themes, personas, journey, activated recs, matrix,
# plan) from a ProjectAnalysis without duplicating the markup. Each returns the
# section HTML, or "" when its data is empty. `accent` params let the Decision
# report re-ink the SVGs bordeaux while the qual report stays green.

def _themes_section_html(themes: list, roster: list, annot_by_theme: dict, L: dict) -> str:
    theme_cards = []
    for i, theme in enumerate(themes):
        quotes_html = ""
        for q in theme.get("quotes", []) or []:
            if isinstance(q, dict):
                text, who = q.get("text", ""), q.get("participant_display_name", "")
                ident = q.get("participant_identifier", "")
                prompt = q.get("question_text", "")
            else:
                text, who, ident, prompt = str(q), "", "", ""
            attribution = " · ".join(x for x in [_esc(ident), _esc(who)] if x)
            prompt_html = f'<div class="quote__prompt">{L["quote_prompt"]}: “{_esc(prompt)}”</div>' if prompt else ""
            unverified_html = (
                f'<div class="quote__unverified">{L["quote_unverified"]}</div>'
                if isinstance(q, dict) and q.get("verified") is False
                else ""
            )
            quotes_html += f"""
            <blockquote class="quote avoid-break">
              <p>“{_esc(text)}”</p>
              <footer>— {attribution or L["anonymous"]}</footer>
              {prompt_html}
              {unverified_html}
            </blockquote>"""

        disconfirm = theme.get("disconfirming_evidence", "")
        disconfirm_html = f"""
            <div class="callout callout--warn avoid-break">
              <div class="callout__title">{L["disconfirming"]}</div>
              <p>{_esc(disconfirm)}</p>
            </div>""" if disconfirm else ""

        rnote = theme.get("researcher_note", "")
        rnote_html = f"""
            <div class="callout callout--note avoid-break">
              <div class="callout__title">{L["researcher_note"]}</div>
              <p>{_esc(rnote)}</p>
            </div>""" if rnote else ""

        annot = annot_by_theme.get(theme.get("title", ""))
        annot_html = ""
        if annot:
            annot_label = L.get(f"annot_{annot.status}", _esc(annot.status))
            note = f' — {_esc(annot.researcher_note)}' if annot.researcher_note else ""
            annot_html = f'<div class="annot annot--{_esc(annot.status)}">{annot_label}{note}</div>'

        theme_cards.append(f"""
        <article class="theme">
          <div class="theme__head avoid-break">
            <span class="theme__num">{i + 1:02d}</span>
            <h3 class="theme__title">{_esc(theme.get("title", ""))}</h3>
            {_freq_meter(theme.get("frequency", ""), L)}
          </div>
          {annot_html}
          <p class="theme__summary">{_esc(theme.get("summary", ""))}</p>
          {quotes_html}
          {disconfirm_html}
          {rnote_html}
        </article>""")

    return f"""
    <section class="page-break">
      <h2 class="section-title">{L["themes_title"]}</h2>
      <p class="section-sub">{L["themes_sub"]}</p>
      {"".join(theme_cards)}
    </section>
    """ if themes else ""


def _evidence_map_html(themes: list, roster: list, L: dict) -> str:
    if not (themes and roster):
        return ""
    head_cells = "".join(
        '<th title="{}">{}</th>'.format(_esc(name), pid)
        for pid, name, _ in roster
    )
    off_dot = '<span class="dot dot--off"></span>'
    body_rows = ""
    for i, theme in enumerate(themes):
        on_dot = '<span class="dot dot--on"></span>'
        quoted = {
            q.get("participant_display_name", "")
            for q in (theme.get("quotes", []) or [])
            if isinstance(q, dict)
        }
        cells = "".join(
            "<td>{}</td>".format(on_dot if (p.display_name or "") in quoted else off_dot)
            for _, _, p in roster
        )
        title_cell = _esc(theme.get("title", ""))
        body_rows += '<tr><td class="ev__theme">{}</td>{}</tr>'.format(title_cell, cells)
    legend = " · ".join(
        "<strong>{}</strong> {}".format(pid, _esc(name))
        for pid, name, _ in roster
    )
    return f"""
    <section class="avoid-break">
      <h2 class="section-title">{L["evidence_map"]}</h2>
      <p class="section-sub">{L["evidence_map_sub"]}</p>
      <table class="ev">
        <thead><tr><th class="ev__theme">{L["theme_col"]}</th>{head_cells}</tr></thead>
        <tbody>{body_rows}</tbody>
      </table>
      <p class="ev__legend">{legend}</p>
    </section>
    """


def _personas_section_html(personas: list, L: dict) -> str:
    def _persona_list(persona: dict, key: str, label: str) -> str:
        items = [x for x in (persona.get(key) or []) if x]
        if not items:
            return ""
        lis = "".join(f"<li>{_esc(x)}</li>" for x in items)
        return f'<div class="persona__list"><h5>{label}</h5><ul>{lis}</ul></div>'

    def _persona_html(p: dict) -> str:
        seg = p.get("segment") or ""
        seg_html = f'<div class="persona__seg">{_esc(seg)}</div>' if seg else ""
        pills = "".join(
            f'<span class="persona__pill">{_esc(x)}</span>'
            for x in (p.get("grounded_in") or []) if x
        )
        from_html = (
            f'<div class="persona__from"><span class="persona__from-label">'
            f'{L["persona_built_from"]}</span>{pills}</div>' if pills else ""
        )
        goals = _persona_list(p, "goals", L["persona_goals"])
        frus = _persona_list(p, "frustrations", L["persona_frustrations"])
        behav = _persona_list(p, "behaviours", L["persona_behaviours"])
        job = (
            f'<p class="persona__job"><b>{L["persona_job"]}</b> {_esc(p.get("primary_job", ""))}</p>'
            if p.get("primary_job") else ""
        )
        aq = p.get("anchor_quote")
        aq_html = ""
        if isinstance(aq, dict) and aq.get("text"):
            unv = (
                f'<div class="quote__unverified">{L["quote_unverified"]}</div>'
                if aq.get("verified") is False else ""
            )
            who = _esc(aq.get("participant_identifier", ""))
            aq_html = (
                f'<blockquote class="quote avoid-break"><p>“{_esc(aq["text"])}”</p>'
                f'<footer>— {who or L["anonymous"]}</footer>{unv}</blockquote>'
            )
        return f"""
        <article class="persona avoid-break">
          <h3 class="persona__name">{_esc(p.get("name", ""))}</h3>
          {seg_html}{from_html}
          <p class="persona__oneliner">{_esc(p.get("one_liner", ""))}</p>
          {goals}{frus}{behav}{job}{aq_html}
        </article>"""

    return f"""
    <section>
      <h2 class="section-title">{L["personas_title"]}</h2>
      <p class="section-sub">{L["personas_sub"]}</p>
      <div class="persona-grid">{"".join(_persona_html(p) for p in personas if isinstance(p, dict))}</div>
    </section>
    """ if personas else ""


def _journey_section_html(journey, L: dict, accent: str) -> str:
    journey_stages = [s for s in (journey.get("stages") or []) if isinstance(s, dict)] \
        if isinstance(journey, dict) else []
    journey_applicable = bool(journey.get("applicable")) if isinstance(journey, dict) else False
    if not (journey_applicable and len(journey_stages) >= 2):
        return ""
    j_svg = _svg_journey_map(journey, L, accent)
    rows = ""
    for s in journey_stages:
        q = s.get("quote") if isinstance(s.get("quote"), dict) else {}
        qtext = (q or {}).get("text", "")
        unv = (
            f' <span class="quote__unverified">{L["quote_unverified"]}</span>'
            if isinstance(q, dict) and q.get("verified") is False else ""
        )
        try:
            emo_str = f"{int(round(float(s.get('emotion', 0)))):+d}"
        except (TypeError, ValueError):
            emo_str = "0"
        qcell = f'“{_esc(qtext)}”{unv}' if qtext else "—"
        rows += (
            f'<tr><td class="journey__stage">{_esc(s.get("name", ""))}</td>'
            f'<td>{_esc(s.get("goal", ""))}</td>'
            f'<td class="journey__emo">{emo_str}</td>'
            f'<td>{qcell}</td>'
            f'<td class="journey__pain">{_esc(s.get("pain", "")) or "—"}</td>'
            f'<td>{_esc(s.get("opportunity", "")) or "—"}</td></tr>'
        )
    jlabel = _esc(journey.get("label", ""))
    jlabel_html = f'<p class="journey__label">{jlabel}</p>' if jlabel else ""
    return f"""
    <section class="avoid-break">
      <h2 class="section-title">{L["journey_title"]}</h2>
      <p class="section-sub">{L["journey_sub"]}</p>
      {jlabel_html}
      {j_svg}
      <table class="journey__table">
        <thead><tr>
          <th>{L["journey_stage"]}</th><th>{L["journey_goal"]}</th><th>{L["journey_emotion"]}</th>
          <th>{L["journey_quote_col"]}</th><th>{L["journey_pain"]}</th><th>{L["journey_opportunity"]}</th>
        </tr></thead>
        <tbody>{rows}</tbody>
      </table>
    </section>
    """


def _jtbd_section_html(jtbds: list, L: dict) -> str:
    jtbd_cards = "".join(f"""
        <article class="jtbd avoid-break">
          <p class="jtbd__job">{_esc(j.get("job", ""))}</p>
          <p class="jtbd__insight"><strong>{L["insight"]}:</strong> {_esc(j.get("insight", ""))}</p>
          {_freq_meter(j.get("frequency", ""), L)}
        </article>""" for j in jtbds)
    return f"""
    <section>
      <h2 class="section-title">{L["jtbd_title"]}</h2>
      <p class="section-sub">{L["jtbd_sub"]}</p>
      <div class="jtbd-grid">{jtbd_cards}</div>
    </section>
    """ if jtbds else ""


def _tensions_section_html(tensions: list, L: dict) -> str:
    tension_cards = "".join(f"""
        <article class="tension avoid-break">
          <h3 class="tension__label">{_esc(t.get("tension", ""))}</h3>
          <p>{_esc(t.get("detail", ""))}</p>
        </article>""" for t in tensions)
    return f"""
    <section>
      <h2 class="section-title">{L["tensions_title"]}</h2>
      <p class="section-sub">{L["tensions_sub"]}</p>
      {tension_cards}
    </section>
    """ if tensions else ""


def _reco_card_html(r, i: int, L: dict) -> str:
    """One recommendation — rich card for objects, plain line for legacy strings."""
    if not isinstance(r, dict):
        return (
            f'<li class="reco avoid-break"><span class="reco__num">{i + 1}</span>'
            f'<p>{_esc(r)}</p></li>'
        )
    horizon = L.get("horizon_" + str(r.get("horizon", "")).replace("-", "_"), "")
    tags = []
    if r.get("owner_role"):
        tags.append(f'<span class="reco__tag"><b>{L["reco_owner"]}</b>{_esc(r["owner_role"])}</span>')
    if horizon:
        tags.append(f'<span class="reco__tag"><b>{L["reco_horizon"]}</b>{horizon}</span>')
    if r.get("impact"):
        lvl = L.get("level_" + str(r["impact"]).lower(), _esc(r["impact"]))
        tags.append(f'<span class="reco__tag reco__tag--impact"><b>{L["impact_label"]}</b>{lvl}</span>')
    if r.get("effort"):
        lvl = L.get("level_" + str(r["effort"]).lower(), _esc(r["effort"]))
        tags.append(f'<span class="reco__tag reco__tag--effort"><b>{L["effort_label"]}</b>{lvl}</span>')
    meta = f'<div class="reco__meta">{"".join(tags)}</div>' if tags else ""
    rationale = f'<p class="reco__rationale">{_esc(r.get("rationale", ""))}</p>' if r.get("rationale") else ""
    kpi = f'<p class="reco__kpi"><b>{L["reco_kpi"]}</b>{_esc(r["kpi"])}</p>' if r.get("kpi") else ""
    fals = f'<p class="reco__falsifier"><b>{L["reco_falsifier"]}</b>{_esc(r["falsifier"])}</p>' if r.get("falsifier") else ""
    return f"""
        <li class="reco reco--rich avoid-break">
          <span class="reco__num">{i + 1}</span>
          <div class="reco__body">
            <p class="reco__action">{_esc(r.get("action", ""))}</p>
            {rationale}{meta}{kpi}{fals}
          </div>
        </li>"""


def _recommendations_section_html(recommendations: list, L: dict) -> str:
    reco_items = "".join(_reco_card_html(r, i, L) for i, r in enumerate(recommendations))
    return f"""
    <section class="avoid-break">
      <h2 class="section-title">{L["recos_title"]}</h2>
      <p class="section-sub">{L["recos_sub"]}</p>
      <ol class="recos">{reco_items}</ol>
    </section>
    """ if recommendations else ""


def _priority_matrix_section_html(recommendations: list, L: dict, accent: str) -> str:
    matrix_svg = _svg_priority_matrix(recommendations, L, accent)
    return f"""
    <section class="avoid-break">
      <h2 class="section-title">{L["matrix_title"]}</h2>
      <p class="section-sub">{L["matrix_sub"]}</p>
      {matrix_svg}
    </section>
    """ if matrix_svg else ""


def _plan_section_html(recommendations: list, L: dict) -> str:
    plan_buckets: dict[str, list] = {"now": [], "30d": [], "60_90d": []}
    for r in recommendations:
        if isinstance(r, dict):
            h = str(r.get("horizon", "")).replace("-", "_")
            if h in plan_buckets:
                plan_buckets[h].append(r)

    def _plan_items(items: list) -> str:
        if not items:
            return '<div class="plan__empty">—</div>'
        out = []
        for it in items:
            owner = it.get("owner_role", "")
            owner_html = f'<span class="plan__owner">{_esc(owner)}</span>' if owner else ""
            out.append(f'<div class="plan__item">{_esc(it.get("action", ""))}{owner_html}</div>')
        return "".join(out)

    plan_cols = "".join(
        f'<div class="plan__col"><div class="plan__when">{L["horizon_" + k]}</div>{_plan_items(plan_buckets[k])}</div>'
        for k in ("now", "30d", "60_90d")
    )
    return f"""
    <section class="avoid-break">
      <h2 class="section-title">{L["plan_title"]}</h2>
      <p class="section-sub">{L["plan_sub"]}</p>
      <div class="plan">{plan_cols}</div>
    </section>
    """ if any(plan_buckets.values()) else ""


def render_analysis_report_html(
    project,
    analysis,
    participants: list,
    annotations: list,
    company_name: str = "",
    include_appendix: bool = True,
) -> str:
    """Build the full standalone HTML document for one analysis version.

    ``include_appendix=False`` is the public-share variant: the participant
    roster (demographics + quality labels) is stripped.
    """
    # The analysis body is written in the owning company's preferred_language
    # (services/analysis.py) — the report chrome must match the body, not the
    # interview language, or an EN researcher running a FR study gets French
    # labels around English analysis text.
    company_lang = getattr(getattr(project, "company", None), "preferred_language", None)
    lang = "fr" if (company_lang or project.language or "en").lower().startswith("fr") else "en"
    L = _STRINGS[lang]

    report = json.loads(analysis.report) if analysis.report else {}
    themes = report.get("themes", []) or []
    jtbds = report.get("jobs_to_be_done", []) or []
    tensions = report.get("tensions", []) or []
    recommendations = report.get("recommendations", []) or []
    personas = report.get("personas", []) or []
    journey = report.get("journey") or {}

    annot_by_theme = {a.theme_title: a for a in annotations}

    # Stable participant ordering: by completion date, P1..Pn identifiers.
    completed = [p for p in participants if p.status == "completed"]
    completed.sort(key=lambda p: (p.completed_at or p.started_at or datetime.min))
    roster = []  # (identifier, display name, participant)
    for i, p in enumerate(completed):
        name = p.display_name or "{} {}".format(L["anonymous"], i + 1)
        roster.append(("P{}".format(i + 1), name, p))

    filters = None
    if analysis.filters:
        try:
            filters = json.loads(analysis.filters)
        except Exception:
            filters = None

    generated = _fmt_date(analysis.generated_at, lang)
    field_start = min((p.completed_at for p in completed if p.completed_at), default=None)
    field_end = max((p.completed_at for p in completed if p.completed_at), default=None)

    # ── header / cover ────────────────────────────────────────────────────
    refined = analysis.version_label == "researcher_refined"
    version_chip = (
        f'<span class="chip">{L["version"].format(v=analysis.version)} · '
        f'{L["refined"] if refined else L["ai_discovery"]}</span>'
    )
    filter_chip = ""
    if filters and filters.get("filter_by"):
        vals = ", ".join(filters.get("filter_values", []))
        filter_chip = f'<span class="chip chip--filter">{L["segment_filter"]}: {_esc(filters["filter_by"])} = {_esc(vals)}</span>'

    header = f"""
    <header class="cover">
      <div class="cover__brand">
        <span class="cover__logo">QualiPulse</span>
        <span class="cover__doctype">{L["doc_type"]}</span>
      </div>
      <h1 class="cover__title">{_esc(project.name)}</h1>
      <div class="cover__meta">
        <span class="chip chip--strong">{len(completed)} {L["interviews"]}</span>
        {_confidence_badge(report.get("confidence", ""), L)}
        {version_chip}
        {filter_chip}
        <span class="chip chip--ghost">{L["generated"]} {generated}</span>
      </div>
    </header>
    """

    # ── executive summary ─────────────────────────────────────────────────
    exec_summary = f"""
    <section class="summary avoid-break">
      <h2 class="eyebrow">{L["exec_summary"]}</h2>
      <p class="summary__text">{_esc(report.get("summary", ""))}</p>
    </section>
    """

    # ── at a glance ───────────────────────────────────────────────────────
    stats = [
        (len(completed), L["stat_participants"]),
        (len(themes), L["stat_themes"]),
        (len(jtbds), L["stat_jtbd"]),
        (len(tensions), L["stat_tensions"]),
        (len(recommendations), L["stat_recos"]),
    ]
    stat_tiles = "".join(
        f'<div class="stat"><div class="stat__num">{n}</div><div class="stat__label">{lbl}</div></div>'
        for n, lbl in stats
    )
    rationale = report.get("confidence_rationale", "")
    glance = f"""
    <section class="avoid-break">
      <h2 class="eyebrow">{L["at_a_glance"]}</h2>
      <div class="stats">{stat_tiles}</div>
      {f'<p class="rationale"><strong>{L["confidence_note"]}:</strong> {_esc(rationale)}</p>' if rationale else ""}
    </section>
    """

    # ── study design ──────────────────────────────────────────────────────
    design_rows = []
    if project.research_objective:
        design_rows.append((L["objective"], _esc(project.research_objective)))
    if getattr(project, "decision_to_inform", None):
        design_rows.append((L["decision"], _esc(project.decision_to_inform)))
    if getattr(project, "target_customer_description", None):
        design_rows.append((L["target"], _esc(project.target_customer_description)))
    design_rows.append((L["duration"], L["duration_value"].format(m=project.interview_duration_minutes)))
    if completed:
        professions = {p.profession for p in completed if p.profession}
        countries = {p.country for p in completed if p.country}
        design_rows.append((
            L["sample_profile"],
            L["sample_profile_value"].format(
                n=len(completed),
                professions=len(professions) or "—",
                countries=len(countries) or "—",
            ),
        ))
    if field_start and field_end:
        window = _fmt_date(field_start, lang) if field_start.date() == field_end.date() \
            else f"{_fmt_date(field_start, lang)} → {_fmt_date(field_end, lang)}"
        design_rows.append((L["field_dates"], window))
    parent_v = None
    if refined and analysis.parent_version_id:
        parent_v = analysis.version - 1  # display hint; exact parent resolved by caller if needed
    lineage = L["lineage_refined"].format(v=analysis.version, p=parent_v or "?") if refined \
        else L["lineage_ai"].format(v=analysis.version)
    design_rows.append((L["analysis_lineage"], lineage))

    design_html = "".join(
        f'<div class="design__row"><dt>{k}</dt><dd>{v}</dd></div>' for k, v in design_rows
    )
    design = f"""
    <section class="avoid-break">
      <h2 class="eyebrow">{L["study_design"]}</h2>
      <dl class="design">{design_html}</dl>
    </section>
    """

    # ── qualitative sections (shared verbatim with the Decision report) ───
    themes_section = _themes_section_html(themes, roster, annot_by_theme, L)
    evidence_section = _evidence_map_html(themes, roster, L)
    personas_section = _personas_section_html(personas, L)
    journey_section = _journey_section_html(journey, L, _accent("qual"))
    jtbd_section = _jtbd_section_html(jtbds, L)
    tensions_section = _tensions_section_html(tensions, L)
    recos_section = _recommendations_section_html(recommendations, L)
    matrix_section = _priority_matrix_section_html(recommendations, L, _accent("qual"))
    plan_section = _plan_section_html(recommendations, L)

    # ── methodology contract ──────────────────────────────────────────────
    contract_items = "".join(f"<li>{item}</li>" for item in L["contract_items"])
    contract = f"""
    <div class="contract avoid-break"><h4>{L["contract_title"]}</h4><ul>{contract_items}</ul></div>
    """

    # ── appendix ──────────────────────────────────────────────────────────
    roster_rows = ""
    for pid, name, p in roster:
        qkey = "quality_{}".format((p.quality_label or "").lower())
        qlabel = L.get(qkey, _esc(p.quality_label or "—"))
        qclass = "q--{}".format(_esc((p.quality_label or "none").lower()))
        roster_rows += f"""
        <tr>
          <td>{pid}</td>
          <td>{_esc(name)}</td>
          <td>{_esc(p.profession) or "—"}</td>
          <td>{_esc(p.age_range) or "—"}</td>
          <td>{_esc(p.country) or "—"}</td>
          <td><span class="q {qclass}">{qlabel}</span></td>
          <td>{_fmt_date(p.completed_at, lang)}</td>
        </tr>"""
    appendix = f"""
    <section class="page-break">
      <h2 class="section-title">{L["appendix"]}</h2>
      <table class="roster">
        <thead><tr>
          <th>{L["col_id"]}</th><th>{L["col_name"]}</th><th>{L["col_profession"]}</th>
          <th>{L["col_age"]}</th><th>{L["col_country"]}</th><th>{L["col_quality"]}</th><th>{L["col_completed"]}</th>
        </tr></thead>
        <tbody>{roster_rows}</tbody>
      </table>
    </section>
    """ if (completed and include_appendix) else ""

    title = f"{project.name} — {L['doc_type']}"

    return f"""<!doctype html>
<html lang="{lang}">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<title>{_esc(title)}</title>
<style>{_ANALYSIS_CSS + _palette_css("qual")}</style>
</head>
<body>
<div class="toolbar"><button onclick="window.print()">{L["print_btn"]}</button></div>
<div class="sheet">
{header}
{exec_summary}
{glance}
{design}
{themes_section}
{evidence_section}
{personas_section}
{journey_section}
{jtbd_section}
{tensions_section}
{recos_section}
{matrix_section}
{plan_section}
{contract}
{appendix}
<footer class="doc-footer">
  <span><strong>QualiPulse</strong> · {_esc(company_name)}</span>
  <span>{L["footer"]}</span>
</footer>
</div>
</body>
</html>"""


# ═══════════════════════════════════════════════════════════════════════════
# Decision memo — cross-study synthesis document
# ═══════════════════════════════════════════════════════════════════════════

_MEMO_STRINGS = {
    "en": {
        "doc_type": "Decision memo",
        "generated": "Generated",
        "studies": "studies",
        "confidence": "Confidence",
        "confidence_low": "Low", "confidence_medium": "Medium", "confidence_high": "High",
        "decision": "Decision under consideration",
        "verdict": "Verdict",
        "summary": "Executive summary",
        "findings": "Key findings across studies",
        "findings_sub": "Every finding names the studies that support it. Strength reflects cross-study corroboration, not enthusiasm.",
        "strength_strong": "Strong evidence",
        "strength_moderate": "Moderate evidence",
        "strength_weak": "Weak evidence",
        "evidence": "Most convincing evidence",
        "conflicts": "Where the studies disagree",
        "conflicts_sub": "Treat these as open questions, not noise — disagreement between studies is information.",
        "gaps": "What we still don't know",
        "recos": "Recommendations",
        "recos_sub": "Decision-oriented next steps. Each one states what evidence would prove it wrong.",
        "stat_studies": "Studies synthesised",
        "stat_interviews": "Interviews behind this memo",
        "stat_findings": "Cross-study findings",
        "stat_conflicts": "Conflicts surfaced",
        "matrix": "Evidence matrix",
        "matrix_sub": "Which studies support each finding. A filled dot marks corroboration in that study's analysis.",
        "col_finding": "Finding",
        "included": "Studies included",
        "col_study": "Study", "col_n": "Interviews", "col_conf": "Confidence",
        "col_date": "Analysed", "col_mixed": "Mixed methods",
        "confidence_note": "How to read this confidence level",
        "contract_title": "Methodology contract",
        "contract_items": [
            "Every finding names the studies that support it; strength reflects cross-study corroboration, never single-study enthusiasm.",
            "Conflicts between studies are reported as first-class findings — disagreement is information, not noise.",
            "Each recommendation states what evidence would prove it wrong.",
            "Synthesised by AI (Claude) from each study's final analyses — raw transcripts are never re-read, so every claim walks back to a study report and from there to a verbatim quote.",
        ],
        "footer": "Generated with QualiPulse — synthesised from per-study analyses with full quote traceability.",
        "print_btn": "Print / Save as PDF",
        "yes": "Yes", "no": "—",
    },
    "fr": {
        "doc_type": "Mémo de décision",
        "generated": "Généré le",
        "studies": "études",
        "confidence": "Confiance",
        "confidence_low": "Faible", "confidence_medium": "Moyenne", "confidence_high": "Élevée",
        "decision": "Décision à éclairer",
        "verdict": "Verdict",
        "summary": "Synthèse",
        "findings": "Enseignements clés transverses",
        "findings_sub": "Chaque enseignement nomme les études qui le soutiennent. La force reflète la corroboration entre études.",
        "strength_strong": "Preuves solides",
        "strength_moderate": "Preuves modérées",
        "strength_weak": "Preuves faibles",
        "evidence": "Preuve la plus convaincante",
        "conflicts": "Là où les études divergent",
        "conflicts_sub": "À traiter comme des questions ouvertes — un désaccord entre études est une information.",
        "gaps": "Ce que nous ne savons toujours pas",
        "recos": "Recommandations",
        "recos_sub": "Prochaines étapes orientées décision. Chacune précise ce qui la réfuterait.",
        "stat_studies": "Études synthétisées",
        "stat_interviews": "Entretiens derrière ce mémo",
        "stat_findings": "Enseignements transverses",
        "stat_conflicts": "Divergences relevées",
        "matrix": "Matrice des preuves",
        "matrix_sub": "Quelles études soutiennent chaque enseignement. Un point plein marque une corroboration dans l'analyse de l'étude.",
        "col_finding": "Enseignement",
        "included": "Études incluses",
        "col_study": "Étude", "col_n": "Entretiens", "col_conf": "Confiance",
        "col_date": "Analysée le", "col_mixed": "Méthodes mixtes",
        "confidence_note": "Comment lire ce niveau de confiance",
        "contract_title": "Contrat méthodologique",
        "contract_items": [
            "Chaque enseignement nomme les études qui le soutiennent ; la force reflète la corroboration entre études, jamais l'enthousiasme d'une seule.",
            "Les désaccords entre études sont rapportés comme des enseignements à part entière — une divergence est une information, pas du bruit.",
            "Chaque recommandation précise ce qui la réfuterait.",
            "Synthétisé par IA (Claude) à partir des analyses finales de chaque étude — les transcripts bruts ne sont jamais relus : chaque affirmation remonte à un rapport d'étude, puis à un verbatim.",
        ],
        "footer": "Généré avec QualiPulse — synthétisé à partir des analyses de chaque étude, avec traçabilité complète des citations.",
        "print_btn": "Imprimer / Enregistrer en PDF",
        "yes": "Oui", "no": "—",
    },
}

_STRENGTH_LEVEL = {"strong": 3, "moderate": 2, "weak": 1}

_MEMO_CSS = _BASE_DOC_CSS + """
/* Flagship treatment: the decision memo opens on a full-ink cover band —
   the boldest document in the system, since it carries everything we know. */
.cover--band { background: var(--accent-deep); color: #fff; border-bottom: 0;
  margin: -56px -40px 0; padding: 72px 40px 52px; }
.cover--band .cover__logo { color: #fff; }
.cover--band .cover__doctype { color: #fff; background: rgba(255,255,255,0.16);
  padding: 3px 10px; border-radius: 3px; letter-spacing: 0.14em; }
.cover--band .cover__title { color: #fff; font-size: 2.5rem; margin: 26px 0 18px; }
.cover--band .cover__meta, .cover--band .cover__meta .chip { color: rgba(255,255,255,0.85); }
.cover--band .cover__meta .chip--strong { color: #fff; }
.cover--band .cover__meta .chip--conf-high, .cover--band .cover__meta .chip--conf-medium,
.cover--band .cover__meta .chip--conf-low { background: rgba(255,255,255,0.16); color: #fff; }
@media (max-width: 720px) { .cover--band { margin: -32px -20px 0; padding: 44px 20px 34px; } }
@media print { .cover--band { margin: 0; padding: 44px 34px 36px; } }

.sec-head { display: flex; align-items: baseline; gap: 14px;
  border-bottom: 1px solid var(--rule); padding-bottom: 8px; margin-bottom: 8px; }
.sec-num { font-family: var(--serif); font-size: 0.95rem; color: var(--ink-3); }
.sec-title { font-size: 12px; font-weight: 700; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--ink); }

.stats--memo { grid-template-columns: repeat(4, 1fr); }
@media (max-width: 720px) { .stats--memo { grid-template-columns: repeat(2, 1fr); } }

.decision-q { font-family: var(--serif); font-size: 1.2rem; font-style: italic;
  color: var(--ink-2); max-width: 62ch; }
.verdict { background: var(--accent-tint); border-left: 3px solid var(--accent);
  padding: 20px 24px; max-width: 680px; }
.verdict p { font-family: var(--serif); font-size: 1.45rem; line-height: 1.42; font-weight: 500; }
.summary-text { font-size: 14.5px; color: var(--ink-2); max-width: 70ch; }

.ev { width: 100%; border-collapse: collapse; font-size: 12.5px; }
.ev th { font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase;
  color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong); padding: 0 8px 6px; text-align: center; }
.ev td { border-bottom: 1px solid var(--rule); padding: 8px; text-align: center; }
.ev__theme { text-align: left !important; font-weight: 600; max-width: 340px; padding-left: 0 !important; }
.dot { display: inline-block; width: 10px; height: 10px; border-radius: 50%; }
.dot--on { background: var(--accent); }
.dot--off { background: var(--rule); width: 6px; height: 6px; }
.ev__legend { font-size: 11.5px; color: var(--ink-3); margin-top: 10px; }

.finding { border-top: 1px solid var(--rule); padding: 20px 0 24px; }
.finding:first-of-type { border-top: 0; padding-top: 6px; }
.finding__head { display: flex; align-items: baseline; gap: 12px; flex-wrap: wrap; margin-bottom: 8px; }
.finding__num { font-family: var(--serif); font-size: 1rem; color: var(--accent);
  font-variant-numeric: tabular-nums; }
.finding__title { font-family: var(--serif); font-size: 1.3rem; font-weight: 600;
  line-height: 1.3; letter-spacing: -0.005em; flex: 1; text-wrap: balance; }
.finding__detail { font-size: 14px; color: var(--ink-2); margin-bottom: 12px; max-width: 74ch; }
.finding__studies { display: flex; flex-wrap: wrap; gap: 6px; margin-bottom: 12px; }
.finding__evidence { border-left: 2px solid var(--rule); padding: 2px 0 2px 14px; }
.finding__evidence .lbl { font-size: 10.5px; font-weight: 700; text-transform: uppercase;
  letter-spacing: 0.08em; color: var(--ink-2); display: block; margin-bottom: 3px; }
.finding__evidence p { font-family: var(--serif); font-size: 1rem; font-style: italic; line-height: 1.5; }

.strength { display: inline-flex; align-items: center; gap: 4px; }
.sdot { width: 7px; height: 7px; border-radius: 50%; background: var(--rule); display: inline-block; }
.sdot--on { background: var(--accent); }
.strength__label { font-size: 11.5px; color: var(--ink-3); margin-left: 4px; }

.conflict { border-left: 3px solid var(--copper); padding: 2px 0 2px 16px; margin-bottom: 18px; }
.conflict h3 { font-family: var(--serif); font-size: 1.02rem; font-weight: 600; margin-bottom: 4px; }
.conflict p { font-size: 13.5px; color: var(--ink-2); max-width: 72ch; }

.gaps { list-style: none; }
.gaps li { border-left: 3px solid var(--amber); background: var(--amber-tint);
  padding: 10px 16px; margin-bottom: 10px; font-size: 13.5px; max-width: 680px; }

.included { width: 100%; border-collapse: collapse; font-size: 12.5px; }
.included th { text-align: left; font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong);
  padding: 0 14px 6px 0; }
.included td { border-bottom: 1px solid var(--rule); padding: 8px 14px 8px 0; vertical-align: top; }
"""


def _strength_meter(strength: str, L: dict) -> str:
    level = _STRENGTH_LEVEL.get((strength or "").lower(), 0)
    label = L.get("strength_{}".format((strength or "").lower()), _esc(strength))
    dots = "".join(
        '<span class="sdot{}"></span>'.format(" sdot--on" if i < level else "")
        for i in range(3)
    )
    return f'<span class="strength" title="{_esc(label)}">{dots}<span class="strength__label">{_esc(label)}</span></span>'


def render_decision_memo_html(synthesis, studies_meta: list, company_name: str = "") -> str:
    """Standalone print-ready HTML for one CrossStudySynthesis decision memo.

    ``studies_meta`` — list of dicts: name, participant_count, confidence,
    generated_at, has_mixed_methods (built by the router from live rows).
    """
    lang = "fr" if (synthesis.language or "en").lower().startswith("fr") else "en"
    L = _MEMO_STRINGS[lang]

    report = json.loads(synthesis.report) if synthesis.report else {}
    findings = report.get("key_findings", []) or []
    conflicts = report.get("conflicts", []) or []
    gaps = report.get("gaps", []) or []
    recommendations = report.get("recommendations", []) or []

    conf = (report.get("confidence", "") or "").lower()
    conf_label = L.get("confidence_{}".format(conf), _esc(report.get("confidence", "")))
    sep = "&nbsp;:" if lang == "fr" else ":"

    header = f"""
    <header class="cover cover--band">
      <div class="cover__brand">
        <span class="cover__logo">QualiPulse</span>
        <span class="cover__doctype">{L["doc_type"]}</span>
      </div>
      <h1 class="cover__title">{_esc(synthesis.name)}</h1>
      <div class="cover__meta">
        <span class="chip chip--strong">{len(studies_meta)} {L["studies"]}</span>
        <span class="chip chip--conf-{_esc(conf)}">{L["confidence"]}{sep} {conf_label}</span>
        <span class="chip chip--ghost">{L["generated"]} {_fmt_date(synthesis.generated_at, lang)}</span>
      </div>
    </header>
    """

    # Key numbers directly under the cover band — the memo leads with the
    # weight of evidence behind it.
    total_interviews = sum(m.get("participant_count") or 0 for m in studies_meta)
    memo_stats = [
        (len(studies_meta), L["stat_studies"]),
        (total_interviews or "—", L["stat_interviews"]),
        (len(findings), L["stat_findings"]),
        (len(conflicts), L["stat_conflicts"]),
    ]
    stat_tiles = "".join(
        f'<div class="stat"><div class="stat__num">{n}</div><div class="stat__label">{lbl}</div></div>'
        for n, lbl in memo_stats
    )
    stat_band = f'<section class="avoid-break"><div class="stats stats--memo">{stat_tiles}</div></section>'

    # Numbered section heads — the counter only advances for sections that
    # actually render, so the numbering never skips.
    _sec_counter = {"n": 0}

    def _head(title_text: str, sub: str = "") -> str:
        _sec_counter["n"] += 1
        sub_html = f'<p class="section-sub">{sub}</p>' if sub else ""
        return (
            f'<div class="sec-head"><span class="sec-num">{_sec_counter["n"]:02d}</span>'
            f'<span class="sec-title">{title_text}</span></div>{sub_html}'
        )

    sections = [stat_band]

    decision_text = report.get("decision", synthesis.decision_question or "")
    if decision_text:
        sections.append(f"""
    <section class="avoid-break">
      {_head(L["decision"])}
      <p class="decision-q">{_esc(decision_text)}</p>
    </section>
    """)

    rationale = report.get("confidence_rationale", "")
    sections.append(f"""
    <section class="avoid-break">
      {_head(L["verdict"])}
      <div class="verdict"><p>{_esc(report.get("verdict", ""))}</p></div>
      {f'<p class="rationale"><strong>{L["confidence_note"]}:</strong> {_esc(rationale)}</p>' if rationale else ""}
    </section>
    """)

    if report.get("summary"):
        sections.append(f"""
    <section class="avoid-break">
      {_head(L["summary"])}
      <p class="summary-text">{_esc(report.get("summary", ""))}</p>
    </section>
    """)

    if findings:
        finding_cards = []
        for i, f in enumerate(findings):
            chips = "".join(
                '<span class="chip">{}</span>'.format(_esc(s))
                for s in (f.get("supporting_studies", []) or [])
            )
            evidence = f.get("evidence", "")
            evidence_html = f"""
          <div class="finding__evidence avoid-break">
            <span class="lbl">{L["evidence"]}</span>
            <p>{_esc(evidence)}</p>
          </div>""" if evidence else ""
            finding_cards.append(f"""
        <article class="finding avoid-break">
          <div class="finding__head">
            <span class="finding__num">{i + 1:02d}</span>
            <h3 class="finding__title">{_esc(f.get("finding", ""))}</h3>
            {_strength_meter(f.get("strength", ""), L)}
          </div>
          <p class="finding__detail">{_esc(f.get("detail", ""))}</p>
          <div class="finding__studies">{chips}</div>
          {evidence_html}
        </article>""")
        sections.append(f"""
    <section>
      {_head(L["findings"], L["findings_sub"])}
      {"".join(finding_cards)}
    </section>
    """)

    # Evidence matrix: findings × studies — the memo's signature exhibit.
    if findings and studies_meta:
        study_names = [m.get("name", "") for m in studies_meta]
        head_cells = "".join(
            '<th title="{}">S{}</th>'.format(_esc(name), i + 1)
            for i, name in enumerate(study_names)
        )
        body_rows = ""
        for i, f in enumerate(findings):
            supporting = set(f.get("supporting_studies", []) or [])
            cells = "".join(
                "<td>{}</td>".format(
                    '<span class="dot dot--on"></span>'
                    if name in supporting
                    else '<span class="dot dot--off"></span>'
                )
                for name in study_names
            )
            label = _truncate_label(f.get("finding", ""), 72)
            body_rows += (
                f'<tr><td class="ev__theme">{i + 1:02d} — {_esc(label)}</td>{cells}</tr>'
            )
        legend = " · ".join(
            "<strong>S{}</strong> {}".format(i + 1, _esc(name))
            for i, name in enumerate(study_names)
        )
        sections.append(f"""
    <section class="avoid-break">
      {_head(L["matrix"], L["matrix_sub"])}
      <table class="ev">
        <thead><tr><th class="ev__theme">{L["col_finding"]}</th>{head_cells}</tr></thead>
        <tbody>{body_rows}</tbody>
      </table>
      <p class="ev__legend">{legend}</p>
    </section>
    """)

    if conflicts:
        conflict_cards = "".join(f"""
        <article class="conflict avoid-break">
          <h3>{_esc(c.get("topic", ""))}</h3>
          <p>{_esc(c.get("detail", ""))}</p>
        </article>""" for c in conflicts)
        sections.append(f"""
    <section>
      {_head(L["conflicts"], L["conflicts_sub"])}
      {conflict_cards}
    </section>
    """)

    if gaps:
        gap_items = "".join(f'<li class="avoid-break">{_esc(g)}</li>' for g in gaps)
        sections.append(f"""
    <section class="avoid-break">
      {_head(L["gaps"])}
      <ul class="gaps">{gap_items}</ul>
    </section>
    """)

    if recommendations:
        reco_items = "".join(f"""
        <li class="reco avoid-break"><span class="reco__num">{i + 1}</span><p>{_esc(r)}</p></li>"""
            for i, r in enumerate(recommendations))
        sections.append(f"""
    <section class="avoid-break">
      {_head(L["recos"], L["recos_sub"])}
      <ol class="recos">{reco_items}</ol>
    </section>
    """)

    contract_items = "".join(f"<li>{item}</li>" for item in L["contract_items"])
    sections.append(f"""
    <div class="contract avoid-break"><h4>{L["contract_title"]}</h4><ul>{contract_items}</ul></div>
    """)

    if studies_meta:
        included_rows = ""
        for m in studies_meta:
            m_conf = (m.get("confidence") or "").lower()
            m_conf_label = L.get("confidence_{}".format(m_conf), "—") if m_conf else "—"
            included_rows += f"""
        <tr>
          <td>{_esc(m.get("name", ""))}</td>
          <td>{m.get("participant_count") if m.get("participant_count") is not None else "—"}</td>
          <td>{m_conf_label}</td>
          <td>{_fmt_date(m.get("generated_at"), lang)}</td>
          <td>{L["yes"] if m.get("has_mixed_methods") else L["no"]}</td>
        </tr>"""
        sections.append(f"""
    <section class="avoid-break">
      {_head(L["included"])}
      <table class="included">
        <thead><tr>
          <th>{L["col_study"]}</th><th>{L["col_n"]}</th><th>{L["col_conf"]}</th>
          <th>{L["col_date"]}</th><th>{L["col_mixed"]}</th>
        </tr></thead>
        <tbody>{included_rows}</tbody>
      </table>
    </section>
    """)

    title = f"{synthesis.name} — {L['doc_type']}"

    return f"""<!doctype html>
<html lang="{lang}">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<title>{_esc(title)}</title>
<style>{_MEMO_CSS + _palette_css("mixed")}</style>
</head>
<body>
<div class="toolbar"><button onclick="window.print()">{L["print_btn"]}</button></div>
<div class="sheet">
{header}
{"".join(sections)}
<footer class="doc-footer">
  <span><strong>QualiPulse</strong> · {_esc(company_name)}</span>
  <span>{L["footer"]}</span>
</footer>
</div>
</body>
</html>"""


# ═══════════════════════════════════════════════════════════════════════════
# Mixed-methods study report — Quantified Themes as a research-grade document
# ═══════════════════════════════════════════════════════════════════════════

_STUDY_STRINGS = {
    "en": {
        "doc_type": "Mixed-methods research report",
        "generated": "Generated",
        "version": "Report v{v}",
        "responses": "survey responses",
        "interviews": "interviews",
        "sec_essentials": "The essentials",
        "sec_evidence": "The evidence at a glance",
        "sec_theme": "Theme {i} · What the evidence says",
        "sec_gaps": "What we don't know yet",
        "sec_methodology": "Methodology, limits & evidence index",
        "verdict_label": "Verdict",
        "counter_label": "What would weaken this",
        "success_label": "Success test",
        "stat_responses": "Completed survey responses",
        "stat_interviews": "Completed interviews",
        "stat_themes": "Quantified themes",
        "stat_surveys": "Survey instruments",
        "stat_validated": "Themes validated by respondents",
        "conf_directional": "Directional",
        "conf_supported": "Supported",
        "conf_strong": "Strong",
        "why_strong": "Why “strong”: {parts} — meets the n≥100, p<0.01-equivalent bar of the methodology contract.",
        "why_supported": "Why “supported”: {parts} — quantitative signal and interviews agree through independent routes.",
        "why_directional": "Why “directional”: {parts} — below inference thresholds or resting on a single method. Treat as a hypothesis to test next.",
        "why_survey_n": "n={n} survey responses",
        "why_segment": "segment “{s}”",
        "why_lift": "{lift}× over-index",
        "why_interviews": "corroborated in {x} interviews",
        "why_survey_only": "survey signal only",
        "survey_signal": "Survey signal",
        "interview_evidence": "Interview evidence · {x}",
        "segments_mentioned": "Raised by: {s}",
        "validation_label": "Respondent validation",
        "validation_agree_pct": "{pct}% agree (95% CI {lo}–{hi}, n={n})",
        "validation_agree_counts": "{agree} of {n} respondents agree — below n=30, raw counts only",
        "validation_awaiting": "Awaiting validation responses for this theme.",
        "reco_product": "Product action",
        "reco_marketing": "Marketing action",
        "reco_next_research": "Next research step",
        "chart_n": "n={n}",
        "chart_no_pct": "Sub-threshold group (n<30) — raw counts, no percentages.",
        "chart_mean": "mean {m}",
        "chart_nps": "NPS {score} · {p} promoters / {pa} passives / {d} detractors",
        "nps_detractors": "Detractors (0–6)",
        "nps_passives": "Passives (7–8)",
        "nps_promoters": "Promoters (9–10)",
        "chart_open_text": "{n} open-text answers — read them in the survey dashboard.",
        "methodology_note": "How this report was produced",
        "contract_title": "Methodology contract",
        "contract_items": [
            "No percentage is shown below n=30; sub-threshold groups are reported as raw counts. Confidence intervals are Wilson 95%.",
            "A theme is “supported” when quantitative signal and interviews agree through independent routes (n≥30 + meaningful effect); “strong” requires n≥100 and a p<0.01-equivalent effect; anything less is “directional”.",
            "Every anchor quote is verified post-generation as a verbatim substring of a real interview transcript; unverifiable quotes are stripped automatically.",
            "Analysis generated by AI (Claude) from survey aggregates and interview transcripts, reviewed by the research team before distribution.",
        ],
        "evidence_index": "Evidence index",
        "idx_surveys": "Surveys",
        "idx_interviews": "Interviews",
        "col_instrument": "Instrument", "col_status": "Status",
        "col_questions": "Questions", "col_completed": "Completed",
        "col_id": "ID", "col_name": "Participant", "col_profession": "Profession",
        "col_country": "Country", "col_turns": "Turns",
        "anonymous": "Participant",
        "footer": "Generated with QualiPulse — survey signal and interview evidence combined with full quote traceability.",
        "print_btn": "Print / Save as PDF",
    },
    "fr": {
        "doc_type": "Rapport de recherche mixte",
        "generated": "Généré le",
        "version": "Rapport v{v}",
        "responses": "réponses au sondage",
        "interviews": "entretiens",
        "sec_essentials": "L'essentiel",
        "sec_evidence": "La preuve en un coup d'œil",
        "sec_theme": "Thème {i} · Ce que dit la preuve",
        "sec_gaps": "Ce que nous ne savons pas encore",
        "sec_methodology": "Méthodologie, limites & index des preuves",
        "verdict_label": "Verdict",
        "counter_label": "Ce qui affaiblirait cette conclusion",
        "success_label": "Test de réussite",
        "stat_responses": "Réponses complètes au sondage",
        "stat_interviews": "Entretiens terminés",
        "stat_themes": "Thèmes quantifiés",
        "stat_surveys": "Questionnaires",
        "stat_validated": "Thèmes validés par les répondants",
        "conf_directional": "Indicatif",
        "conf_supported": "Étayé",
        "conf_strong": "Solide",
        "why_strong": "Pourquoi « solide » : {parts} — atteint le seuil n≥100 et un effet équivalent p<0,01 du contrat méthodologique.",
        "why_supported": "Pourquoi « étayé » : {parts} — le signal quantitatif et les entretiens concordent par des voies indépendantes.",
        "why_directional": "Pourquoi « indicatif » : {parts} — sous les seuils d'inférence ou fondé sur une seule méthode. À traiter comme une hypothèse à tester.",
        "why_survey_n": "n={n} réponses au sondage",
        "why_segment": "segment « {s} »",
        "why_lift": "sur-représentation ×{lift}",
        "why_interviews": "corroboré dans {x} entretiens",
        "why_survey_only": "signal du sondage uniquement",
        "survey_signal": "Signal du questionnaire",
        "interview_evidence": "Preuve d'entretien · {x}",
        "segments_mentioned": "Évoqué par : {s}",
        "validation_label": "Validation par les répondants",
        "validation_agree_pct": "{pct}% d'accord (IC 95% {lo}–{hi}, n={n})",
        "validation_agree_counts": "{agree} répondants sur {n} d'accord — sous n=30, effectifs bruts uniquement",
        "validation_awaiting": "En attente des réponses de validation pour ce thème.",
        "reco_product": "Action produit",
        "reco_marketing": "Action marketing",
        "reco_next_research": "Prochaine étape de recherche",
        "chart_n": "n={n}",
        "chart_no_pct": "Groupe sous le seuil (n<30) — effectifs bruts, pas de pourcentages.",
        "chart_mean": "moyenne {m}",
        "chart_nps": "NPS {score} · {p} promoteurs / {pa} passifs / {d} détracteurs",
        "nps_detractors": "Détracteurs (0–6)",
        "nps_passives": "Passifs (7–8)",
        "nps_promoters": "Promoteurs (9–10)",
        "chart_open_text": "{n} réponses libres — à lire dans le tableau de bord du questionnaire.",
        "methodology_note": "Comment ce rapport a été produit",
        "contract_title": "Contrat méthodologique",
        "contract_items": [
            "Aucun pourcentage affiché sous n=30 ; les sous-groupes sont rapportés en effectifs bruts. Les intervalles de confiance sont des Wilson à 95%.",
            "Un thème est « étayé » quand signal quantitatif et entretiens concordent par des voies indépendantes (n≥30 + effet significatif) ; « solide » exige n≥100 et un effet équivalent p<0,01 ; en deçà, il est « indicatif ».",
            "Chaque citation d'ancrage est vérifiée après génération comme extrait verbatim d'un vrai transcript d'entretien ; les citations invérifiables sont retirées automatiquement.",
            "Analyse générée par IA (Claude) à partir des agrégats du sondage et des transcripts d'entretien, relue par l'équipe de recherche avant diffusion.",
        ],
        "evidence_index": "Index des preuves",
        "idx_surveys": "Questionnaires",
        "idx_interviews": "Entretiens",
        "col_instrument": "Instrument", "col_status": "Statut",
        "col_questions": "Questions", "col_completed": "Complétées",
        "col_id": "ID", "col_name": "Participant", "col_profession": "Profession",
        "col_country": "Pays", "col_turns": "Tours",
        "anonymous": "Participant",
        "footer": "Généré avec QualiPulse — signal quantitatif et preuves d'entretien combinés, avec traçabilité complète des citations.",
        "print_btn": "Imprimer / Enregistrer en PDF",
    },
}

# Serif-led, evergreen document design — deliberately closer to a consulting
# memo than to the app UI. Self-contained (no webfonts), print-exact colors.
_STUDY_CSS = """
:root {
  --paper: #fcfcfa; --ink: #17201b; --ink-2: #4c5852; --ink-3: #7a847e;
  --rule: #dfe5e0; --rule-strong: #17201b;
  --accent: #1d5c3f; --accent-tint: #eef4ef;
  --copper: #a2541f; --amber: #8a5a17; --amber-tint: #f6efdf;
  --gap-bg: #edefed;
  --serif: "Iowan Old Style", "Palatino Linotype", Palatino, "Charter", Georgia, serif;
  --sans: "Avenir Next", "Segoe UI", -apple-system, "Helvetica Neue", sans-serif;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
html { -webkit-print-color-adjust: exact; print-color-adjust: exact; }
body { font-family: var(--sans); color: var(--ink); background: var(--paper);
  font-size: 15px; line-height: 1.6; }
.sheet { max-width: 780px; margin: 0 auto; padding: 56px 40px 64px; }
@media (max-width: 720px) { .sheet { padding: 32px 20px; } }
.num, .tabular { font-variant-numeric: tabular-nums; }

.eyebrow { font-size: 11px; font-weight: 600; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--accent); }
.cover { border-bottom: 2px solid var(--rule-strong); padding-bottom: 24px; }
.cover__brand { display: flex; align-items: baseline; gap: 12px; }
.cover__logo { font-weight: 700; font-size: 1rem; color: var(--accent); letter-spacing: -0.01em; }
.cover__title { font-family: var(--serif); font-size: 2.1rem; font-weight: 600;
  line-height: 1.22; letter-spacing: -0.01em; margin: 12px 0 14px; text-wrap: balance; }
.cover__meta { display: flex; flex-wrap: wrap; gap: 6px 24px; font-size: 12.5px; color: var(--ink-2); }
.cover__meta strong { color: var(--ink); font-weight: 600; }

section { margin-top: 46px; }
.sec-head { display: flex; align-items: baseline; gap: 14px;
  border-bottom: 1px solid var(--rule); padding-bottom: 8px; margin-bottom: 20px; }
.sec-num { font-family: var(--serif); font-size: 0.95rem; color: var(--ink-3); }
.sec-title { font-size: 12px; font-weight: 700; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--ink); }

.verdict { font-family: var(--serif); font-size: 1.4rem; line-height: 1.45;
  font-weight: 500; margin-bottom: 22px; max-width: 60ch; text-wrap: balance; }
.findings { display: flex; flex-direction: column; gap: 14px; }
.finding { display: grid; grid-template-columns: 28px 1fr; gap: 12px; align-items: start; }
.finding__num { font-family: var(--serif); font-size: 1.15rem; color: var(--accent); }
.finding p { margin: 2px 0 0; }

.chip { display: inline-block; font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; padding: 2px 8px; border-radius: 3px; white-space: nowrap;
  vertical-align: 1px; }
.chip--strong, .chip--supported { background: var(--accent-tint); color: var(--accent); }
.chip--directional { background: var(--amber-tint); color: var(--amber); }
.chip--gap { background: var(--gap-bg); color: var(--ink-2); }

.stat-band { display: grid; grid-template-columns: repeat(4, 1fr); gap: 22px; margin-bottom: 30px; }
@media (max-width: 640px) { .stat-band { grid-template-columns: repeat(2, 1fr); } }
.stat__value { font-family: var(--serif); font-size: 1.9rem; font-weight: 600;
  line-height: 1.1; font-variant-numeric: tabular-nums; }
.stat__label { font-size: 11.5px; color: var(--ink-2); margin-top: 4px; line-height: 1.4; }

figure { margin: 0 0 26px; }
.chart-title { font-size: 13px; font-weight: 600; margin-bottom: 2px; max-width: 70ch; }
.fig-takeaway { font-family: var(--serif); font-size: 1.02rem; line-height: 1.45;
  margin: 2px 0 6px; max-width: 66ch; }
.chart-meta { font-size: 11.5px; color: var(--ink-3); margin-bottom: 10px; }
.chart-wrap { overflow-x: auto; }
figcaption { font-size: 12px; color: var(--ink-2); margin-top: 6px; max-width: 70ch; }
.survey-block > h3 { font-size: 12px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--ink-2); margin: 26px 0 16px; }

.theme-title { font-family: var(--serif); font-size: 1.35rem; font-weight: 600;
  line-height: 1.3; margin-bottom: 6px; text-wrap: balance; }
.theme-title .chip { margin-left: 10px; }
.confidence-why { font-size: 12.5px; color: var(--ink-2); margin-bottom: 18px; max-width: 74ch; }
.evidence-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 26px; margin: 6px 0 16px; }
@media (max-width: 640px) { .evidence-grid { grid-template-columns: 1fr; } }
.evidence-col h4 { font-size: 11px; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--ink-2); margin-bottom: 10px; }
.signal-summary { font-family: var(--serif); font-size: 1.05rem; line-height: 1.45; margin-bottom: 6px; }
.signal-meta { font-size: 12px; color: var(--ink-3); }
blockquote { margin: 0 0 10px; padding-left: 14px; border-left: 2px solid var(--rule); }
blockquote p { font-family: var(--serif); font-size: 1rem; font-style: italic;
  line-height: 1.5; margin-bottom: 4px; }
blockquote footer { font-size: 12px; color: var(--ink-2); }
.validation-row { border-top: 1px solid var(--rule); padding-top: 12px; margin-top: 4px;
  font-size: 13px; display: flex; align-items: baseline; gap: 10px; flex-wrap: wrap; }
.validation-row .lbl { font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--ink-2); }
.validation-bar { display: inline-block; width: 120px; height: 8px; background: var(--gap-bg);
  border-radius: 4px; overflow: hidden; vertical-align: middle; }
.validation-bar i { display: block; height: 100%; background: var(--accent); }
.so-what { background: var(--accent-tint); border-left: 3px solid var(--accent);
  padding: 14px 18px; margin-top: 14px; max-width: 660px; }
.so-what .lbl { font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--accent); display: block; margin-bottom: 4px; }
.so-what p { font-size: 13.5px; margin: 0; }
.so-what .rationale-line { color: var(--ink-2); font-size: 12.5px; margin-top: 4px; }
.verdict-box { margin-top: 22px; }
.counter-box { background: var(--copper-tint, #f7eee5); border-left: 3px solid var(--copper);
  padding: 12px 16px; margin-bottom: 14px; max-width: 660px; }
.counter-box .lbl { font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--copper); display: block; margin-bottom: 4px; }
.counter-box p { font-size: 13px; margin: 0; }
.gaps { list-style: none; margin: 0; padding: 0; }
.gaps li { border-left: 3px solid var(--amber); background: var(--amber-tint);
  padding: 10px 16px; margin-bottom: 10px; font-size: 13.5px; max-width: 680px; }

.method-note { font-size: 13.5px; max-width: 74ch; margin-bottom: 8px; }
.contract { background: #f4f6f4; padding: 16px 20px; font-size: 13px; margin-top: 18px; max-width: 680px; }
.contract h4 { font-size: 11px; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--ink-2); margin-bottom: 8px; }
.contract ul { margin: 0; padding-left: 18px; }
.contract li { margin-bottom: 5px; }
.idx { margin-top: 24px; }
.idx h4 { font-size: 11px; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--ink-2); margin-bottom: 10px; }
.idx table { border-collapse: collapse; width: 100%; font-size: 12.5px; margin-bottom: 18px; }
.idx th { text-align: left; font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong);
  padding: 0 14px 6px 0; }
.idx td { border-bottom: 1px solid var(--rule); padding: 8px 14px 8px 0; vertical-align: top; }

.doc-footer { margin-top: 52px; border-top: 1px solid var(--rule); padding-top: 14px;
  font-size: 12px; color: var(--ink-3); display: flex; justify-content: space-between;
  flex-wrap: wrap; gap: 8px; }
.doc-footer strong { color: var(--accent); }
.toolbar { position: fixed; top: 18px; right: 18px; z-index: 10; }
.toolbar button { font: inherit; font-size: 0.82rem; font-weight: 600; color: #fff;
  background: var(--accent); border: 0; border-radius: 999px; padding: 10px 18px;
  cursor: pointer; box-shadow: 0 4px 16px rgba(23,32,27,0.18); }
.toolbar button:hover { background: #16452f; }
.avoid-break { break-inside: avoid; page-break-inside: avoid; }
@media (max-width: 600px) {
  .toolbar { position: static; display: flex; justify-content: flex-end; padding: 16px 20px 0; }
}
svg text { font-family: var(--sans); }
@media print {
  body { background: #fff; font-size: 12.5px; }
  .sheet { max-width: none; padding: 0; }
  .toolbar { display: none; }
  section { margin-top: 30px; }
  @page { size: A4; margin: 16mm 14mm; }
}
"""


def _truncate_label(text: str, limit: int = 36) -> str:
    text = text or ""
    return text if len(text) <= limit else text[: limit - 1].rstrip() + "…"


def _svg_choice_bars(choices: list[dict], lang: str, accent: str = "#1d5c3f") -> str:
    """Horizontal bar chart for a multiple-choice breakdown.

    Counts are always printed on the bar; percentages (+ Wilson CI) only
    when the analytics layer released them (n>=30) — the renderer never
    computes a percentage itself.
    """
    rows = [c for c in choices if c.get("label") is not None]
    if not rows:
        return ""
    max_count = max((c.get("count") or 0) for c in rows) or 1
    row_h, label_w, bar_max = 28, 250, 260
    height = len(rows) * row_h + 6
    parts = [
        f'<svg viewBox="0 0 680 {height}" width="100%" role="img">',
    ]
    for i, c in enumerate(rows):
        y = i * row_h
        count = c.get("count") or 0
        w = max(2, round(bar_max * count / max_count)) if count else 0
        opacity = max(0.35, 1 - i * 0.16)
        label = _esc(_truncate_label(str(c.get("label"))))
        pct = c.get("percentage")
        if pct is not None:
            value = f"{count} · {round(pct)}%"
            ci_low, ci_high = c.get("ci_low"), c.get("ci_high")
            if ci_low is not None and ci_high is not None:
                value += f" [{round(ci_low)}–{round(ci_high)}]"
        else:
            value = str(count)
        parts.append(f'<text x="0" y="{y + 18}" font-size="12" fill="#17201b">{label}</text>')
        if count:
            parts.append(
                f'<rect x="{label_w}" y="{y + 8}" width="{w}" height="13" '
                f'fill="{accent}" opacity="{opacity:.2f}"/>'
            )
        parts.append(
            f'<text x="{label_w + w + 8}" y="{y + 19}" font-size="11.5" '
            f'font-weight="600" fill="#4c5852">{_esc(value)}</text>'
        )
    parts.append("</svg>")
    return "".join(parts)


def _svg_nps_band(breakdown: dict, L: dict, accent: str = "#1d5c3f") -> str:
    """NPS composition band: score + detractor/passive/promoter stacked bar.

    Only rendered when the analytics layer released the NPS decomposition
    (n>=30); segment widths come straight from the released counts.
    """
    promoters = breakdown.get("promoters")
    passives = breakdown.get("passives")
    detractors = breakdown.get("detractors")
    score = breakdown.get("nps_score")
    if score is None or promoters is None or passives is None or detractors is None:
        return ""
    total = (promoters or 0) + (passives or 0) + (detractors or 0)
    if total <= 0:
        return ""
    bar_x, bar_w, bar_y, bar_h = 128, 552, 16, 24
    seg = [
        (detractors, "#a2541f", L["nps_detractors"]),
        (passives, "#b9c2bb", L["nps_passives"]),
        (promoters, accent, L["nps_promoters"]),
    ]
    parts = [
        '<svg viewBox="0 0 680 84" width="100%" role="img">',
        f'<text x="0" y="{bar_y + 22}" font-size="30" font-weight="700" '
        f'fill="{accent}" font-family="Georgia, serif">{score:+d}</text>',
        f'<text x="0" y="{bar_y + 40}" font-size="10.5" fill="#7a847e">NPS</text>',
    ]
    x = float(bar_x)
    for count, color, label in seg:
        w = bar_w * count / total
        if count:
            parts.append(
                f'<rect x="{x:.1f}" y="{bar_y}" width="{max(w, 1.5):.1f}" '
                f'height="{bar_h}" fill="{color}"/>'
            )
            if w >= 26:
                parts.append(
                    f'<text x="{x + w / 2:.1f}" y="{bar_y + 16}" font-size="11" '
                    f'font-weight="600" text-anchor="middle" fill="#ffffff">{count}</text>'
                )
        x += w
    # segment legend under the bar
    lx = bar_x
    for count, color, label in seg:
        parts.append(f'<rect x="{lx}" y="{bar_y + 36}" width="9" height="9" fill="{color}"/>')
        parts.append(
            f'<text x="{lx + 14}" y="{bar_y + 44}" font-size="11" fill="#4c5852">'
            f"{_esc(label)} · {count}</text>"
        )
        lx += 184
    parts.append("</svg>")
    return "".join(parts)


def _svg_histogram(histogram: list[dict], mean: float | None, accent: str = "#1d5c3f") -> str:
    """Column chart for a likert / NPS bucket histogram (counts always shown)."""
    buckets = [(h.get("bucket"), h.get("count") or 0) for h in histogram]
    if not buckets:
        return ""
    max_count = max(c for _, c in buckets) or 1
    n_buckets = len(buckets)
    plot_w, base_y, plot_h, left = 620, 130, 105, 30
    slot = plot_w / n_buckets
    bar_w = min(34, slot * 0.6)
    parts = [
        f'<svg viewBox="0 0 680 165" width="100%" role="img">',
        f'<line x1="{left}" y1="{base_y}" x2="{left + plot_w}" y2="{base_y}" stroke="#dfe5e0"/>',
    ]
    for i, (bucket, count) in enumerate(buckets):
        cx = left + slot * i + slot / 2
        h = round(plot_h * count / max_count) if count else 0
        if count:
            parts.append(
                f'<rect x="{cx - bar_w / 2:.1f}" y="{base_y - h}" width="{bar_w:.1f}" '
                f'height="{h}" fill="{accent}"/>'
            )
            parts.append(
                f'<text x="{cx:.1f}" y="{base_y - h - 5}" font-size="10.5" '
                f'text-anchor="middle" fill="#4c5852">{count}</text>'
            )
        parts.append(
            f'<text x="{cx:.1f}" y="{base_y + 16}" font-size="11" '
            f'text-anchor="middle" fill="#7a847e">{_esc(bucket)}</text>'
        )
    if mean is not None and n_buckets > 1:
        first = buckets[0][0]
        last = buckets[-1][0]
        try:
            span = float(last) - float(first)
            if span > 0:
                mx = left + slot / 2 + (float(mean) - float(first)) / span * (plot_w - slot)
                parts.append(
                    f'<line x1="{mx:.1f}" y1="18" x2="{mx:.1f}" y2="{base_y}" '
                    f'stroke="#a2541f" stroke-width="1" stroke-dasharray="3 3"/>'
                )
                parts.append(
                    f'<text x="{mx:.1f}" y="12" font-size="10.5" text-anchor="middle" '
                    f'fill="#a2541f">{float(mean):.1f}</text>'
                )
        except (TypeError, ValueError):
            pass
    parts.append("</svg>")
    return "".join(parts)


def _svg_priority_matrix(recommendations: list, L: dict, accent: str = "#1d5c3f") -> str:
    """Impact × effort 2×2 for object recommendations (deterministic, no LLM).

    Plots each dict recommendation as a numbered dot: X = effort (low on the
    left = more feasible), Y = impact (high at the top). Identical cells are
    jittered so dots don't fully overlap. Legend maps numbers → actions.
    """
    recs = [r for r in recommendations
            if isinstance(r, dict) and r.get("impact") and r.get("effort")]
    if len(recs) < 2:
        return ""
    frac = {"low": 0.2, "medium": 0.5, "high": 0.8}
    PL, PR, PT, PB = 70, 430, 26, 300

    def _x(effort):
        return PL + frac.get(str(effort).lower(), 0.5) * (PR - PL)

    def _y(impact):
        return PB - frac.get(str(impact).lower(), 0.5) * (PB - PT)

    cells: dict = {}
    for i, r in enumerate(recs):
        cells.setdefault((str(r["impact"]).lower(), str(r["effort"]).lower()), []).append(i)

    off = [(0, 0), (13, 0), (-13, 0), (0, 13), (0, -13), (11, 11), (-11, -11), (11, -11), (-11, 11)]
    midx, midy = (PL + PR) / 2, (PT + PB) / 2
    legend_bottom = PT + 6 + len(recs) * 18
    height = int(max(PB + 40, legend_bottom + 6))
    parts = [
        f'<svg viewBox="0 0 680 {height}" width="100%" role="img" aria-label="{_esc(L["matrix_title"])}">',
        f'<rect x="{PL}" y="{PT}" width="{PR - PL}" height="{PB - PT}" fill="none" stroke="#dfe5e0"/>',
        f'<line x1="{midx}" y1="{PT}" x2="{midx}" y2="{PB}" stroke="#eef0ee"/>',
        f'<line x1="{PL}" y1="{midy}" x2="{PR}" y2="{midy}" stroke="#eef0ee"/>',
        f'<text x="{PL + 8}" y="{PT + 16}" font-size="10.5" fill="#7a847e">{L["quad_quickwins"]}</text>',
        f'<text x="{PR - 8}" y="{PT + 16}" font-size="10.5" text-anchor="end" fill="#7a847e">{L["quad_bigbets"]}</text>',
        f'<text x="{PL + 8}" y="{PB - 8}" font-size="10.5" fill="#7a847e">{L["quad_fillins"]}</text>',
        f'<text x="{PR - 8}" y="{PB - 8}" font-size="10.5" text-anchor="end" fill="#7a847e">{L["quad_moneypits"]}</text>',
        f'<text x="{midx:.0f}" y="{PB + 26}" font-size="11" text-anchor="middle" fill="#4c5852">{L["matrix_x"]}</text>',
        f'<text x="{PL - 16}" y="{midy:.0f}" font-size="11" text-anchor="middle" fill="#4c5852" '
        f'transform="rotate(-90 {PL - 16} {midy:.0f})">{L["matrix_y"]}</text>',
    ]
    for (impact, effort), idxs in cells.items():
        bx, by = _x(effort), _y(impact)
        for j, i in enumerate(idxs):
            ox, oy = off[j % len(off)]
            cx, cy = bx + ox, by + oy
            parts.append(f'<circle cx="{cx:.1f}" cy="{cy:.1f}" r="11" fill="{accent}"/>')
            parts.append(
                f'<text x="{cx:.1f}" y="{cy + 3.6:.1f}" font-size="11" font-weight="700" '
                f'text-anchor="middle" fill="#ffffff">{i + 1}</text>'
            )
    ly = PT + 6
    for i, r in enumerate(recs):
        parts.append(
            f'<text x="470" y="{ly}" font-size="11" fill="#4c5852">'
            f'<tspan font-weight="700" fill="{accent}">{i + 1}.</tspan> '
            f'{_esc(_truncate_label(r.get("action", ""), 30))}</text>'
        )
        ly += 18
    parts.append("</svg>")
    return "".join(parts)


def _svg_journey_map(journey: dict, L: dict, accent: str = "#1d5c3f") -> str:
    """Emotion arc across journey stages (the signature journey exhibit).

    Stage names sit across the top; a polyline traces emotion (-2..+2) with
    dots coloured by valence. The full per-stage detail (goal / quote / pain /
    opportunity) lives in the HTML table beside this SVG, so nothing is locked
    inside the graphic.
    """
    stages = [s for s in (journey.get("stages") or []) if isinstance(s, dict)]
    if len(stages) < 2:
        return ""

    def _emo(s) -> int:
        try:
            return max(-2, min(2, int(round(float(s.get("emotion", 0))))))
        except (TypeError, ValueError):
            return 0

    n = len(stages)
    x0, x1 = 30, 672
    col_w = (x1 - x0) / n
    BT, BB = 40, 156
    mid = (BT + BB) / 2
    unit = (BB - BT) / 4

    def _y(e):
        return mid - e * unit

    def _cx(i):
        return x0 + col_w * (i + 0.5)

    height = BB + 30
    parts = [
        f'<svg viewBox="0 0 680 {height}" width="100%" role="img" '
        f'aria-label="{_esc(L["journey_emotion"])}">'
    ]
    for e, lbl in ((2, "+2"), (0, "0"), (-2, "-2")):
        y = _y(e)
        dash = ' stroke-dasharray="3 3"' if e == 0 else ""
        parts.append(f'<line x1="{x0}" y1="{y:.1f}" x2="{x1}" y2="{y:.1f}" stroke="#e6eae6"{dash}/>')
        parts.append(f'<text x="{x0 - 8}" y="{y + 3:.1f}" font-size="9.5" text-anchor="end" fill="#a7afa9">{lbl}</text>')
    for i, s in enumerate(stages):
        if i:
            sx = x0 + col_w * i
            parts.append(f'<line x1="{sx:.1f}" y1="{BT - 18}" x2="{sx:.1f}" y2="{BB}" stroke="#f0f2f0"/>')
        parts.append(
            f'<text x="{_cx(i):.1f}" y="20" font-size="11" font-weight="700" '
            f'text-anchor="middle" fill="#17201b">{_esc(_truncate_label(s.get("name", ""), 16))}</text>'
        )
    pts = [(_cx(i), _y(_emo(s))) for i, s in enumerate(stages)]
    area = (f'{pts[0][0]:.1f},{BB} ' + " ".join(f"{x:.1f},{y:.1f}" for x, y in pts)
            + f' {pts[-1][0]:.1f},{BB}')
    parts.append(f'<polygon points="{area}" fill="{accent}" opacity="0.10"/>')
    parts.append(f'<polyline points="{" ".join(f"{x:.1f},{y:.1f}" for x, y in pts)}" '
                 f'fill="none" stroke="{accent}" stroke-width="2"/>')
    for i, s in enumerate(stages):
        e = _emo(s)
        cx, cy = pts[i]
        color = "#a2541f" if e < 0 else (accent if e > 0 else "#7a847e")
        parts.append(f'<circle cx="{cx:.1f}" cy="{cy:.1f}" r="6" fill="{color}"/>')
        parts.append(
            f'<text x="{cx:.1f}" y="{cy - 11:.1f}" font-size="10" font-weight="700" '
            f'text-anchor="middle" fill="{color}">{e:+d}</text>'
        )
    parts.append(
        f'<text x="{(x0 + x1) / 2:.0f}" y="{BB + 22}" font-size="10.5" '
        f'text-anchor="middle" fill="#7a847e">{_esc(L["journey_emotion"])}</text>'
    )
    parts.append("</svg>")
    return "".join(parts)


def _study_confidence_chip(confidence: str, L: dict) -> str:
    c = (confidence or "").lower()
    label = L.get(f"conf_{c}", _esc(confidence))
    return f'<span class="chip chip--{_esc(c)}">{label}</span>'


def _confidence_why(theme: dict, L: dict) -> str:
    """Deterministic one-line rationale for a theme's confidence pill.

    Built from the same fields the calibration thresholds run on (n,
    segment, interview corroboration) — no AI involved, so the rationale
    can never drift from the data.
    """
    parts = []
    signal = theme.get("survey_signal") or {}
    evidence = theme.get("interview_evidence") or {}
    if signal.get("n"):
        parts.append(L["why_survey_n"].format(n=signal["n"]))
    if signal.get("segment_label"):
        parts.append(L["why_segment"].format(s=signal["segment_label"]))
    if signal.get("segment_over_index"):
        parts.append(L["why_lift"].format(lift=round(signal["segment_over_index"], 1)))
    if evidence.get("x_of_y"):
        parts.append(L["why_interviews"].format(x=evidence["x_of_y"]))
    elif signal:
        parts.append(L["why_survey_only"])
    joined = _esc(", ".join(parts)) if parts else "—"
    template = L.get(f"why_{(theme.get('confidence') or '').lower()}")
    if not template:
        return ""
    return f'<p class="confidence-why">{template.format(parts=joined)}</p>'


def _theme_validation_row(snapshot, L: dict) -> str:
    """Respondent-validation line for one theme (Wilson-CI or raw counts)."""
    if snapshot is None or snapshot.n_answered == 0:
        return (
            f'<div class="validation-row"><span class="lbl">{L["validation_label"]}</span>'
            f'<span class="chip chip--gap">{L["validation_awaiting"]}</span></div>'
        )
    if snapshot.agreement_pct is not None:
        pct = round(snapshot.agreement_pct)
        text = L["validation_agree_pct"].format(
            pct=pct,
            lo=round(snapshot.ci_low) if snapshot.ci_low is not None else "—",
            hi=round(snapshot.ci_high) if snapshot.ci_high is not None else "—",
            n=snapshot.n_answered,
        )
        bar = f'<span class="validation-bar"><i style="width:{pct}%"></i></span>'
    else:
        agree = (snapshot.distribution or {}).get(4, 0) + (snapshot.distribution or {}).get(5, 0)
        text = L["validation_agree_counts"].format(agree=agree, n=snapshot.n_answered)
        width = round(100 * agree / snapshot.n_answered) if snapshot.n_answered else 0
        bar = f'<span class="validation-bar"><i style="width:{width}%"></i></span>'
    return (
        f'<div class="validation-row"><span class="lbl">{L["validation_label"]}</span>'
        f'{bar}<span class="num">{_esc(text)}</span></div>'
    )


# ── Decision report (approach B): qual sections + survey + integration ──────
# Composes the SAME qualitative exhibits (from a ProjectAnalysis, via the shared
# _*_section_html helpers) with the survey charts and a light integration layer,
# so the combined document is a strict superset of the qualitative and survey
# reports. Bordeaux identity (the flagship). Chrome reuses _STUDY_STRINGS with a
# small decision-specific overlay; qual sections use _STRINGS; survey figures use
# _SURVEY_STRINGS.
_DECISION_STRINGS = {
    "en": {
        "doc_type": "Decision report",
        "survey_title": "Survey evidence",
        "survey_sub": "What the numbers confirm — or complicate — in the interviews.",
        "joint_title": "Where the numbers meet the voices",
        "joint_sub": "Each qualitative theme, checked against the survey signal.",
        "joint_theme": "Theme", "joint_signal": "Survey signal",
        "joint_conf": "Confidence", "joint_counter": "What weakens it",
        "gaps_title": "What we still can't answer",
        "footer": "Generated with QualiPulse — the decision report: interviews, survey, and the call they point to.",
    },
    "fr": {
        "doc_type": "Rapport de décision",
        "survey_title": "Signal quantitatif",
        "survey_sub": "Ce que les chiffres confirment — ou compliquent — dans les entretiens.",
        "joint_title": "Quand les chiffres rejoignent les voix",
        "joint_sub": "Chaque thème qualitatif, confronté au signal du sondage.",
        "joint_theme": "Thème", "joint_signal": "Signal du sondage",
        "joint_conf": "Confiance", "joint_counter": "Ce qui l'affaiblit",
        "gaps_title": "Ce qu'on ne peut pas encore trancher",
        "footer": "Généré avec QualiPulse — le rapport de décision : entretiens, sondage et la décision qu'ils éclairent.",
    },
}

_DECISION_EXTRA_CSS = """
.decision-verdict { background: var(--accent-tint); border-left: 3px solid var(--accent);
  padding: 18px 22px; max-width: 680px; margin-bottom: 6px; }
.decision-verdict .lbl { font-size: 10.5px; font-weight: 700; letter-spacing: 0.1em;
  text-transform: uppercase; color: var(--accent); }
.decision-verdict p { font-family: var(--serif); font-size: 1.3rem; line-height: 1.45;
  font-weight: 500; margin-top: 6px; }
.q-block { margin: 20px 0 24px; break-inside: avoid; }
.chart-title { font-family: var(--serif); font-size: 1.02rem; font-weight: 600; margin-bottom: 8px; }
.chart-meta { font-size: 11.5px; color: var(--ink-3); margin-top: 6px; }
.fig-takeaway { font-size: 13px; color: var(--ink-2); margin-top: 6px; }
.samples { margin: 8px 0 0; padding-left: 16px; }
.samples .quote { border: 0; padding: 2px 0; margin: 4px 0; }
.joint { width: 100%; border-collapse: collapse; font-size: 12.5px; }
.joint th { text-align: left; font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong);
  padding: 0 10px 6px 0; }
.joint td { border-bottom: 1px solid var(--rule); padding: 8px 10px 8px 0; vertical-align: top; }
.joint__counter { color: var(--copper); }
.gaps-list { margin: 6px 0 0; padding: 0; list-style: none; }
.gaps-list li { border-left: 3px solid var(--amber); background: var(--amber-tint);
  padding: 8px 14px; margin: 6px 0; font-size: 13px; }
.idx-table { width: 100%; border-collapse: collapse; font-size: 12.5px; margin: 8px 0 20px; }
.idx-table th { text-align: left; font-size: 10.5px; font-weight: 700; letter-spacing: 0.08em;
  text-transform: uppercase; color: var(--ink-2); border-bottom: 1.5px solid var(--rule-strong);
  padding: 0 14px 6px 0; }
.idx-table td { border-bottom: 1px solid var(--rule); padding: 8px 14px 8px 0; }
"""


def render_decision_report_html(
    study,
    project_analysis,
    dashboards: list,
    integration: dict | None = None,
    participants: list | None = None,
    turn_counts: dict | None = None,
    company_name: str = "",
    include_toolbar: bool = True,
) -> str:
    """Superset "Decision report": qual exhibits (from ProjectAnalysis) + survey
    charts (from dashboards) + integration (verdict / joint display / gaps).

    ``project_analysis`` — the interview ProjectAnalysis (its .report supplies
    themes/personas/journey/activated-recs); may be None (survey-only study).
    ``dashboards`` — SurveyDashboardPayload list (survey charts).
    ``integration`` — {verdict, confidence, joint_display[], gaps[]} from the
    integration pass (Phase 3); optional — the qual + survey layers stand alone.
    """
    participants = participants or []
    turn_counts = turn_counts or {}
    integration = integration or {}

    company_lang = getattr(getattr(study, "company", None), "preferred_language", None)
    lang = "fr" if (company_lang or "en").lower().startswith("fr") else "en"
    Lq = _STRINGS[lang]                                   # qual section labels
    Ls = {**_STUDY_STRINGS[lang], **_DECISION_STRINGS[lang]}  # chrome
    Lf = _SURVEY_STRINGS[lang]                            # survey-figure labels

    qual = json.loads(project_analysis.report) if (project_analysis and project_analysis.report) else {}
    themes = qual.get("themes", []) or []
    personas = qual.get("personas", []) or []
    journey = qual.get("journey") or {}
    jtbds = qual.get("jobs_to_be_done", []) or []
    tensions = qual.get("tensions", []) or []
    recommendations = qual.get("recommendations", []) or []

    completed = [p for p in participants if p.status == "completed"]
    completed.sort(key=lambda p: (p.completed_at or p.started_at or datetime.min))
    roster = [("P{}".format(i + 1), (p.display_name or "{} {}".format(Lq["anonymous"], i + 1)), p)
              for i, p in enumerate(completed)]

    accent = _accent("mixed")
    n_responses = sum(getattr(d, "n_completed", 0) for d in dashboards)
    n_interviews = len(completed)
    generated = _fmt_date(getattr(project_analysis, "generated_at", None), lang)

    # ── header ────────────────────────────────────────────────────────────
    header = f"""
    <header class="cover">
      <div class="cover__brand">
        <span class="cover__logo">QualiPulse</span>
        <span class="cover__doctype">{Ls["doc_type"]}</span>
      </div>
      <h1 class="cover__title">{_esc(study.name)}</h1>
      <div class="cover__meta">
        <span class="chip chip--strong">{n_responses} {Ls["responses"]}</span>
        <span class="chip chip--strong">{n_interviews} {Ls["interviews"]}</span>
        <span class="chip chip--ghost">{Ls["generated"]} {generated}</span>
      </div>
    </header>
    """

    # ── verdict (integration) ─────────────────────────────────────────────
    verdict = integration.get("verdict")
    verdict_html = ""
    if verdict:
        conf = integration.get("confidence")
        chip = _study_confidence_chip(conf, Ls) if conf else ""
        verdict_html = f"""
    <section class="avoid-break">
      <div class="decision-verdict">
        <span class="lbl">{Ls["verdict_label"]}</span> {chip}
        <p>{_esc(verdict)}</p>
      </div>
    </section>
    """

    # ── qualitative sections (shared helpers; bordeaux SVGs) ──────────────
    themes_section = _themes_section_html(themes, roster, {}, Lq)
    evidence_section = _evidence_map_html(themes, roster, Lq)
    personas_section = _personas_section_html(personas, Lq)
    journey_section = _journey_section_html(journey, Lq, accent)
    jtbd_section = _jtbd_section_html(jtbds, Lq)
    tensions_section = _tensions_section_html(tensions, Lq)
    recos_section = _recommendations_section_html(recommendations, Lq)
    matrix_section = _priority_matrix_section_html(recommendations, Lq, accent)
    plan_section = _plan_section_html(recommendations, Lq)

    # ── survey evidence (charts) ──────────────────────────────────────────
    figures = "".join(
        _survey_question_figure(q, Lf, lang, accent)
        for dash in dashboards for q in dash.questions
        if getattr(q, "n_answered", 0)
    )
    survey_section = f"""
    <section>
      <h2 class="section-title">{Ls["survey_title"]}</h2>
      <p class="section-sub">{Ls["survey_sub"]}</p>
      {figures}
    </section>
    """ if figures else ""

    # ── joint display (integration): theme × survey signal ────────────────
    jd = integration.get("joint_display") or []
    joint_section = ""
    if jd:
        rows = "".join(
            f'<tr><td>{_esc(x.get("theme_title", ""))}</td>'
            f'<td>{_esc(x.get("survey_signal", "") or "—")}</td>'
            f'<td>{_study_confidence_chip(x.get("confidence", ""), Ls) if x.get("confidence") else "—"}</td>'
            f'<td class="joint__counter">{_esc(x.get("counter_evidence", "") or "—")}</td></tr>'
            for x in jd
        )
        joint_section = f"""
    <section class="avoid-break">
      <h2 class="section-title">{Ls["joint_title"]}</h2>
      <p class="section-sub">{Ls["joint_sub"]}</p>
      <table class="joint">
        <thead><tr><th>{Ls["joint_theme"]}</th><th>{Ls["joint_signal"]}</th>
          <th>{Ls["joint_conf"]}</th><th>{Ls["joint_counter"]}</th></tr></thead>
        <tbody>{rows}</tbody>
      </table>
    </section>
    """

    # ── gaps (integration) ────────────────────────────────────────────────
    gaps = [g for g in (integration.get("gaps") or []) if g]
    gaps_section = ""
    if gaps:
        lis = "".join(f"<li>{_esc(g)}</li>" for g in gaps)
        gaps_section = f"""
    <section class="avoid-break">
      <h2 class="section-title">{Ls["gaps_title"]}</h2>
      <ul class="gaps-list">{lis}</ul>
    </section>
    """

    # ── methodology + evidence index ──────────────────────────────────────
    contract_items = "".join(f"<li>{item}</li>" for item in Ls["contract_items"])
    contract = f'<div class="contract avoid-break"><h4>{Ls["contract_title"]}</h4><ul>{contract_items}</ul></div>'

    survey_rows = "".join(
        f'<tr><td>{_esc(getattr(d, "name", ""))}</td><td>{_esc(getattr(d, "status", ""))}</td>'
        f'<td>{len(d.questions)}</td><td>{getattr(d, "n_completed", 0)}</td></tr>'
        for d in dashboards
    )
    survey_index = f"""
      <h3 class="eyebrow">{Ls["idx_surveys"]}</h3>
      <table class="idx-table"><thead><tr>
        <th>{Ls["col_instrument"]}</th><th>{Ls["col_status"]}</th>
        <th>{Ls["col_questions"]}</th><th>{Ls["col_completed"]}</th></tr></thead>
      <tbody>{survey_rows}</tbody></table>
    """ if dashboards else ""

    part_rows = "".join(
        f'<tr><td>{pid}</td><td>{_esc(name)}</td><td>{_esc(p.profession) or "—"}</td>'
        f'<td>{_esc(p.country) or "—"}</td><td>{turn_counts.get(p.id, "—")}</td></tr>'
        for pid, name, p in roster
    )
    part_index = f"""
      <h3 class="eyebrow">{Ls["idx_interviews"]}</h3>
      <table class="idx-table"><thead><tr>
        <th>{Ls["col_id"]}</th><th>{Ls["col_name"]}</th><th>{Ls["col_profession"]}</th>
        <th>{Ls["col_country"]}</th><th>{Ls["col_turns"]}</th></tr></thead>
      <tbody>{part_rows}</tbody></table>
    """ if roster else ""

    methodology = f"""
    <section class="page-break">
      <h2 class="section-title">{Ls["sec_methodology"]}</h2>
      {contract}
      {survey_index}
      {part_index}
    </section>
    """

    toolbar = f'<div class="toolbar"><button onclick="window.print()">{Ls["print_btn"]}</button></div>' if include_toolbar else ""
    title = f"{study.name} — {Ls['doc_type']}"
    return f"""<!doctype html>
<html lang="{lang}">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<title>{_esc(title)}</title>
<style>{_ANALYSIS_CSS + _palette_css("mixed") + _DECISION_EXTRA_CSS}</style>
</head>
<body>
{toolbar}
<div class="sheet">
{header}
{verdict_html}
{themes_section}
{evidence_section}
{personas_section}
{journey_section}
{jtbd_section}
{tensions_section}
{survey_section}
{joint_section}
{recos_section}
{matrix_section}
{plan_section}
{gaps_section}
{methodology}
<footer class="doc-footer">
  <span><strong>QualiPulse</strong> · {_esc(company_name)}</span>
  <span>{Ls["footer"]}</span>
</footer>
</div>
</body>
</html>"""


def render_study_report_html(
    study,
    analysis,
    dashboards: list,
    validation,
    participants: list,
    turn_counts: dict,
    company_name: str = "",
    include_toolbar: bool = True,
) -> str:
    """Standalone print-ready HTML for one StudyAnalysis (Quantified Themes).

    Everything rendered is either straight from the stored report JSON or
    deterministically computed from the same aggregates the generation
    pipeline used (``build_dashboard`` / ``compute_validation_summary``) —
    the renderer never invents numbers and never shows a percentage the
    stats layer suppressed.

    ``dashboards``   — SurveyDashboardPayload list (validation surveys excluded).
    ``validation``   — ValidationSummary | None for this analysis.
    ``participants`` — completed Participants across the study's projects.
    ``turn_counts``  — participant_id → number of turns (evidence index).
    ``include_toolbar`` — False for the in-app embedded view, where the
    surrounding page owns the export affordances.
    """
    company_lang = getattr(getattr(study, "company", None), "preferred_language", None)
    lang = "fr" if (company_lang or "en").lower().startswith("fr") else "en"
    L = _STUDY_STRINGS[lang]

    report = json.loads(analysis.report) if analysis.report else {}
    themes = report.get("themes", []) or []

    n_responses = report.get("generated_with_response_count") or sum(
        d.n_completed for d in dashboards
    )
    n_interviews = report.get("generated_with_interview_count") or len(participants)
    generated = _fmt_date(analysis.generated_at, lang)

    # ── header ────────────────────────────────────────────────────────────
    header = f"""
    <header class="cover">
      <div class="cover__brand">
        <span class="cover__logo">QualiPulse</span>
        <span class="eyebrow">{L["doc_type"]}</span>
      </div>
      <h1 class="cover__title">{_esc(study.name)}</h1>
      <div class="cover__meta num">
        <span><strong>{n_responses}</strong> {L["responses"]}</span>
        <span><strong>{n_interviews}</strong> {L["interviews"]}</span>
        <span>{L["version"].format(v=analysis.version)}</span>
        <span>{L["generated"]} {generated}</span>
      </div>
    </header>
    """

    # ── 1. essentials: verdict + numbered findings ────────────────────────
    findings = "".join(
        f"""<div class="finding">
          <span class="finding__num num">{i + 1}.</span>
          <p><strong>{_esc(t.get("title", ""))}</strong> {_study_confidence_chip(t.get("confidence", ""), L)}</p>
        </div>"""
        for i, t in enumerate(themes)
    )
    verdict_html = ""
    if report.get("verdict"):
        verdict_html = (
            f'<div class="so-what verdict-box"><span class="lbl">{L["verdict_label"]}</span>'
            f'<p><strong>{_esc(report["verdict"])}</strong></p></div>'
        )
    essentials = f"""
    <section class="avoid-break">
      <div class="sec-head"><span class="sec-num num">1</span><span class="sec-title">{L["sec_essentials"]}</span></div>
      <p class="verdict">{_esc(report.get("executive_summary", ""))}</p>
      <div class="findings">{findings}</div>
      {verdict_html}
    </section>
    """

    # ── 2. evidence at a glance: stat band + per-question charts ─────────
    validated_count = 0
    if validation is not None:
        validated_count = sum(
            1 for s in validation.per_theme.values() if s.n_answered > 0
        )
    if validation is not None and validated_count:
        stat4 = f'<div class="stat"><div class="stat__value num">{validated_count}/{len(themes)}</div><div class="stat__label">{L["stat_validated"]}</div></div>'
    else:
        stat4 = f'<div class="stat"><div class="stat__value num">{len(dashboards)}</div><div class="stat__label">{L["stat_surveys"]}</div></div>'
    stat_band = f"""
    <div class="stat-band">
      <div class="stat"><div class="stat__value num">{n_responses}</div><div class="stat__label">{L["stat_responses"]}</div></div>
      <div class="stat"><div class="stat__value num">{n_interviews}</div><div class="stat__label">{L["stat_interviews"]}</div></div>
      <div class="stat"><div class="stat__value num">{len(themes)}</div><div class="stat__label">{L["stat_themes"]}</div></div>
      {stat4}
    </div>
    """

    accent = _accent("mixed")
    chart_blocks = []
    for dash in dashboards:
        figures = []
        for q in dash.questions:
            if q.n_answered == 0:
                continue
            meta_bits = [L["chart_n"].format(n=q.n_answered)]
            takeaway_html = (
                f'<p class="fig-takeaway">{_esc(q.takeaway)}</p>'
                if getattr(q, "takeaway", None)
                else ""
            )
            body = ""
            caption = ""
            if q.type in ("likert", "nps"):
                hist = (q.breakdown or {}).get("histogram") or []
                if not any((h.get("count") or 0) for h in hist):
                    continue
                if q.mean is not None:
                    meta_bits.append(L["chart_mean"].format(m=f"{q.mean:.1f}"))
                body = _svg_histogram(hist, q.mean, accent)
                nps_band = _svg_nps_band(q.breakdown or {}, L, accent)
                if nps_band:
                    body += nps_band
                elif (q.breakdown or {}).get("nps_score") is not None:
                    caption = L["chart_nps"].format(
                        score=(q.breakdown or {}).get("nps_score"),
                        p=(q.breakdown or {}).get("promoters", 0),
                        pa=(q.breakdown or {}).get("passives", 0),
                        d=(q.breakdown or {}).get("detractors", 0),
                    )
            elif q.type in ("mc_single", "mc_multi"):
                choices = (q.breakdown or {}).get("choices") or []
                if not choices:
                    continue
                body = _svg_choice_bars(choices, lang, accent)
                if all(c.get("percentage") is None for c in choices):
                    caption = L["chart_no_pct"]
            elif q.type in ("open_text", "short_text"):
                total = (q.breakdown or {}).get("total_texts", q.n_answered)
                figures.append(
                    f'<figure class="avoid-break"><div class="chart-title">{_esc(q.prompt)}</div>'
                    f"{takeaway_html}"
                    f'<figcaption>{L["chart_open_text"].format(n=total)}</figcaption></figure>'
                )
                continue
            if not body:
                continue
            caption_html = f"<figcaption>{_esc(caption)}</figcaption>" if caption else ""
            figures.append(
                f'<figure class="avoid-break"><div class="chart-title">{_esc(q.prompt)}</div>'
                f"{takeaway_html}"
                f'<div class="chart-meta num">{_esc(" · ".join(meta_bits))}</div>'
                f'<div class="chart-wrap">{body}</div>{caption_html}</figure>'
            )
        if figures:
            chart_blocks.append(
                f'<div class="survey-block"><h3>{_esc(dash.name)}</h3>{"".join(figures)}</div>'
            )
    evidence = f"""
    <section>
      <div class="sec-head"><span class="sec-num num">2</span><span class="sec-title">{L["sec_evidence"]}</span></div>
      {stat_band}
      {"".join(chart_blocks)}
    </section>
    """

    # ── 3..N theme sections ───────────────────────────────────────────────
    theme_sections = []
    for i, theme in enumerate(themes):
        signal = theme.get("survey_signal") or {}
        ev = theme.get("interview_evidence") or {}
        reco = theme.get("recommendation") or {}

        cols = []
        if signal:
            meta = [L["chart_n"].format(n=signal.get("n", "—"))]
            if signal.get("segment_label"):
                meta.append(_esc(signal["segment_label"]))
            if signal.get("segment_over_index"):
                meta.append(L["why_lift"].format(lift=round(signal["segment_over_index"], 1)))
            cols.append(
                f'<div class="evidence-col"><h4>{L["survey_signal"]}</h4>'
                f'<p class="signal-summary">{_esc(signal.get("summary", ""))}</p>'
                f'<p class="signal-meta num">{" · ".join(meta)}</p></div>'
            )
        if ev and ev.get("anchor_quote"):
            segments = ", ".join(ev.get("segments_mentioned") or [])
            seg_line = (
                f'<footer>{_esc(L["segments_mentioned"].format(s=segments))}</footer>'
                if segments
                else ""
            )
            cols.append(
                f'<div class="evidence-col"><h4>{_esc(L["interview_evidence"].format(x=ev.get("x_of_y", "")))}</h4>'
                f'<blockquote><p>«&nbsp;{_esc(ev["anchor_quote"])}&nbsp;»</p>{seg_line}</blockquote></div>'
            )

        counter_html = ""
        if theme.get("counter_evidence"):
            counter_html = (
                f'<div class="counter-box"><span class="lbl">{L["counter_label"]}</span>'
                f'<p>{_esc(theme["counter_evidence"])}</p></div>'
            )

        validation_html = ""
        if validation is not None:
            validation_html = _theme_validation_row(validation.per_theme.get(i), L)

        reco_html = ""
        if reco.get("action"):
            kind_label = L.get(f"reco_{(reco.get('kind') or '').lower()}", "")
            rationale = (
                f'<p class="rationale-line">{_esc(reco.get("rationale") or "")}</p>'
                if reco.get("rationale")
                else ""
            )
            success = (
                f'<p class="rationale-line"><strong>{L["success_label"]}&nbsp;:</strong> {_esc(reco["success_test"])}</p>'
                if reco.get("success_test") and lang == "fr"
                else f'<p class="rationale-line"><strong>{L["success_label"]}:</strong> {_esc(reco["success_test"])}</p>'
                if reco.get("success_test")
                else ""
            )
            reco_html = (
                f'<div class="so-what"><span class="lbl">{kind_label}</span>'
                f'<p><strong>{_esc(reco["action"])}</strong></p>{rationale}{success}</div>'
            )

        theme_sections.append(f"""
        <section class="avoid-break">
          <div class="sec-head"><span class="sec-num num">{i + 3}</span><span class="sec-title">{L["sec_theme"].format(i=i + 1)}</span></div>
          <h2 class="theme-title">{_esc(theme.get("title", ""))}{_study_confidence_chip(theme.get("confidence", ""), L)}</h2>
          {_confidence_why(theme, L)}
          <div class="evidence-grid">{"".join(cols)}</div>
          {counter_html}
          {validation_html}
          {reco_html}
        </section>
        """)

    # ── gaps: what the data cannot answer ─────────────────────────────────
    gaps = report.get("gaps") or []
    next_sec = len(themes) + 3
    gaps_section = ""
    if gaps:
        gap_items = "".join(f'<li class="avoid-break">{_esc(g)}</li>' for g in gaps)
        gaps_section = f"""
    <section class="avoid-break">
      <div class="sec-head"><span class="sec-num num">{next_sec}</span><span class="sec-title">{L["sec_gaps"]}</span></div>
      <ul class="gaps">{gap_items}</ul>
    </section>
    """
        next_sec += 1

    # ── methodology + evidence index ──────────────────────────────────────
    contract_items = "".join(f"<li>{item}</li>" for item in L["contract_items"])
    survey_rows = "".join(
        f"<tr><td>{_esc(d.name)}</td><td>{_esc(d.status)}</td>"
        f'<td class="num">{len(d.questions)}</td><td class="num">{d.n_completed}</td></tr>'
        for d in dashboards
    )
    surveys_table = (
        f'<h4>{L["idx_surveys"]}</h4><table><thead><tr>'
        f'<th>{L["col_instrument"]}</th><th>{L["col_status"]}</th>'
        f'<th>{L["col_questions"]}</th><th>{L["col_completed"]}</th>'
        f"</tr></thead><tbody>{survey_rows}</tbody></table>"
        if survey_rows
        else ""
    )
    participant_rows = "".join(
        f'<tr><td class="num">E-{i + 1:02d}</td>'
        f'<td>{_esc(p.display_name or L["anonymous"])}</td>'
        f"<td>{_esc(p.profession or '—')}</td><td>{_esc(p.country or '—')}</td>"
        f'<td class="num">{turn_counts.get(p.id, "—")}</td></tr>'
        for i, p in enumerate(participants)
    )
    interviews_table = (
        f'<h4>{L["idx_interviews"]}</h4><table><thead><tr>'
        f'<th>{L["col_id"]}</th><th>{L["col_name"]}</th><th>{L["col_profession"]}</th>'
        f'<th>{L["col_country"]}</th><th>{L["col_turns"]}</th>'
        f"</tr></thead><tbody>{participant_rows}</tbody></table>"
        if participant_rows
        else ""
    )
    methodology_note = report.get("methodology_note") or ""
    methodology = f"""
    <section>
      <div class="sec-head"><span class="sec-num num">{next_sec}</span><span class="sec-title">{L["sec_methodology"]}</span></div>
      <p class="method-note">{_esc(methodology_note)}</p>
      <div class="contract avoid-break"><h4>{L["contract_title"]}</h4><ul>{contract_items}</ul></div>
      <div class="idx">{surveys_table}{interviews_table}</div>
    </section>
    """

    title = f"{study.name} — {L['doc_type']}"
    toolbar = (
        f'<div class="toolbar"><button onclick="window.print()">{L["print_btn"]}</button></div>'
        if include_toolbar
        else ""
    )
    return f"""<!doctype html>
<html lang="{lang}">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<title>{_esc(title)}</title>
<style>{_STUDY_CSS + _palette_css("mixed")}</style>
</head>
<body>
{toolbar}
<div class="sheet">
{header}
{essentials}
{evidence}
{"".join(theme_sections)}
{gaps_section}
{methodology}
<footer class="doc-footer">
  <span><strong>QualiPulse</strong> · {_esc(company_name)}</span>
  <span>{L["footer"]}</span>
</footer>
</div>
</body>
</html>"""


# ══════════════════════════════════════════════════════════════════════
# Standalone survey-results report (quantitative dashboard export)
# ══════════════════════════════════════════════════════════════════════
# Same evergreen document language as the study/analysis/memo exports, but
# driven purely by a SurveyDashboardPayload (build_dashboard). Charts are
# drawn server-side with the shared _svg_histogram / _svg_choice_bars
# helpers, so the document always matches the dashboard's actual data and
# never shows a percentage the stats layer suppressed (n<30 → counts only).

_SURVEY_STRINGS = {
    "en": {
        "doc_type": "Survey results report",
        "responses": "completed responses",
        "started_meta": "started",
        "completion_meta": "completion",
        "exported": "Exported",
        "fielding": "Fielding",
        "sec_glance": "Results at a glance",
        "sec_questions": "Results by question",
        "sec_methodology": "Methodology & limits",
        "stat_completed": "Completed responses",
        "stat_started": "Responses started",
        "stat_completion": "Completion rate",
        "stat_questions": "Questions",
        "status_label": "Status",
        "chart_n": "n={n}",
        "chart_mean": "mean {m}",
        "chart_nps": "NPS {score} · {p} promoters / {pa} passives / {d} detractors",
        "nps_detractors": "Detractors (0–6)",
        "nps_passives": "Passives (7–8)",
        "nps_promoters": "Promoters (9–10)",
        "chart_no_pct": "Sub-threshold group (n<30) — raw counts, no percentages.",
        "open_text_total": "{n} open-text answers",
        "open_text_sample": "Sample of verbatim answers",
        "no_answers": "No responses to this question yet.",
        "below_threshold": "Below n={n}",
        "note_below_threshold": "Total responses are below the n={n} threshold for inference — percentages are suppressed and raw counts are shown instead.",
        "note_wilson": "Percentages carry Wilson 95% confidence intervals. Groups below n={n} are reported as raw counts only, never as percentages.",
        "contract_title": "Methodology contract",
        "contract_items": [
            "No percentage is shown below n=30; sub-threshold groups are reported as raw counts. Confidence intervals are Wilson 95%.",
            "Open-text answers are sampled, not coded — read the full set in the survey dashboard.",
            "Every figure is drawn directly from the survey's stored responses; the renderer never computes or invents a value.",
        ],
        "footer": "Generated with QualiPulse — quantitative survey signal with a transparent methodology contract.",
        "print_btn": "Print / Save as PDF",
    },
    "fr": {
        "doc_type": "Rapport de résultats du sondage",
        "responses": "réponses complètes",
        "started_meta": "commencées",
        "completion_meta": "complétion",
        "exported": "Exporté le",
        "fielding": "Collecte",
        "sec_glance": "Les résultats en un coup d'œil",
        "sec_questions": "Résultats par question",
        "sec_methodology": "Méthodologie & limites",
        "stat_completed": "Réponses complètes",
        "stat_started": "Réponses commencées",
        "stat_completion": "Taux de complétion",
        "stat_questions": "Questions",
        "status_label": "Statut",
        "chart_n": "n={n}",
        "chart_mean": "moyenne {m}",
        "chart_nps": "NPS {score} · {p} promoteurs / {pa} passifs / {d} détracteurs",
        "nps_detractors": "Détracteurs (0–6)",
        "nps_passives": "Passifs (7–8)",
        "nps_promoters": "Promoteurs (9–10)",
        "chart_no_pct": "Groupe sous le seuil (n<30) — effectifs bruts, pas de pourcentages.",
        "open_text_total": "{n} réponses libres",
        "open_text_sample": "Échantillon de réponses verbatim",
        "no_answers": "Aucune réponse à cette question pour l'instant.",
        "below_threshold": "Sous n={n}",
        "note_below_threshold": "Le nombre total de réponses est sous le seuil n={n} pour l'inférence — les pourcentages sont masqués et les effectifs bruts sont affichés à la place.",
        "note_wilson": "Les pourcentages sont assortis d'intervalles de confiance de Wilson à 95%. Les groupes sous n={n} sont rapportés en effectifs bruts uniquement, jamais en pourcentages.",
        "contract_title": "Contrat méthodologique",
        "contract_items": [
            "Aucun pourcentage affiché sous n=30 ; les sous-groupes sont rapportés en effectifs bruts. Les intervalles de confiance sont des Wilson à 95%.",
            "Les réponses libres sont échantillonnées, non codées — consultez l'ensemble dans le tableau de bord du sondage.",
            "Chaque figure est tracée directement à partir des réponses stockées du sondage ; le moteur de rendu ne calcule ni n'invente aucune valeur.",
        ],
        "footer": "Généré avec QualiPulse — signal quantitatif de sondage avec un contrat méthodologique transparent.",
        "print_btn": "Imprimer / Enregistrer en PDF",
    },
}

# Evergreen tokens from _BASE_DOC_CSS + the section-head / stat-band / chart
# scaffolding the survey document needs (mirrors _STUDY_CSS's chart classes).
_SURVEY_CSS = _BASE_DOC_CSS + """
.num, .tabular { font-variant-numeric: tabular-nums; }
.cover__brand { display: flex; align-items: baseline; gap: 12px; margin-bottom: 4px; }

.sec-head { display: flex; align-items: baseline; gap: 14px;
  border-bottom: 1px solid var(--rule); padding-bottom: 8px; margin-bottom: 20px; }
.sec-num { font-family: var(--serif); font-size: 0.95rem; color: var(--ink-3); }
.sec-title { font-size: 12px; font-weight: 700; letter-spacing: 0.12em;
  text-transform: uppercase; color: var(--ink); }

.stat-band { display: grid; grid-template-columns: repeat(4, 1fr); gap: 22px; margin-bottom: 26px; }
@media (max-width: 640px) { .stat-band { grid-template-columns: repeat(2, 1fr); } }
.stat__value { font-family: var(--serif); font-size: 1.9rem; font-weight: 600;
  line-height: 1.1; font-variant-numeric: tabular-nums; }

.method-note { font-size: 13.5px; color: var(--ink-2); max-width: 74ch; margin-bottom: 8px; }

figure { margin: 0 0 26px; }
.chart-title { font-size: 13px; font-weight: 600; margin-bottom: 2px; max-width: 70ch; }
.fig-takeaway { font-family: var(--serif); font-size: 1.02rem; line-height: 1.45;
  margin: 2px 0 6px; max-width: 66ch; }
.chart-meta { font-size: 11.5px; color: var(--ink-3); margin-bottom: 10px; }
.chart-wrap { overflow-x: auto; }
figcaption { font-size: 12px; color: var(--ink-2); margin-top: 6px; max-width: 70ch; }
.q-block { border-top: 1px solid var(--rule); padding-top: 22px; }
.q-block:first-of-type { border-top: 0; padding-top: 0; }
.samples { list-style: none; margin: 8px 0 0; padding: 0; }
.samples .quote { margin: 0 0 10px; }
svg text { font-family: var(--sans); }
"""


def _survey_question_figure(q, L: dict, lang: str, accent: str = "#1e4a73") -> str:
    """Render one dashboard question as a titled figure.

    Reuses the same server-side SVG helpers and suppression rules as the
    study report: counts are always shown, percentages only when the
    analytics layer released them (n>=30).
    """
    title = f'<div class="chart-title">{_esc(q.prompt)}</div>'
    takeaway_html = (
        f'<p class="fig-takeaway">{_esc(q.takeaway)}</p>'
        if getattr(q, "takeaway", None)
        else ""
    )
    if q.n_answered == 0:
        return (
            f'<figure class="q-block avoid-break">{title}'
            f'<figcaption>{L["no_answers"]}</figcaption></figure>'
        )

    meta_bits = [L["chart_n"].format(n=q.n_answered)]
    body = ""
    caption = ""

    if q.type in ("likert", "nps"):
        hist = (q.breakdown or {}).get("histogram") or []
        if not any((h.get("count") or 0) for h in hist):
            return (
                f'<figure class="q-block avoid-break">{title}'
                f'<figcaption>{L["no_answers"]}</figcaption></figure>'
            )
        if q.mean is not None:
            meta_bits.append(L["chart_mean"].format(m=f"{q.mean:.1f}"))
        body = _svg_histogram(hist, q.mean, accent)
        nps_band = _svg_nps_band(q.breakdown or {}, L, accent)
        if nps_band:
            body += nps_band
        elif (q.breakdown or {}).get("nps_score") is not None:
            caption = L["chart_nps"].format(
                score=(q.breakdown or {}).get("nps_score"),
                p=(q.breakdown or {}).get("promoters", 0),
                pa=(q.breakdown or {}).get("passives", 0),
                d=(q.breakdown or {}).get("detractors", 0),
            )
    elif q.type in ("mc_single", "mc_multi"):
        choices = (q.breakdown or {}).get("choices") or []
        if not choices:
            return (
                f'<figure class="q-block avoid-break">{title}'
                f'<figcaption>{L["no_answers"]}</figcaption></figure>'
            )
        body = _svg_choice_bars(choices, lang, accent)
        if all(c.get("percentage") is None for c in choices):
            caption = L["chart_no_pct"]
    elif q.type in ("open_text", "short_text"):
        breakdown = q.breakdown or {}
        total = breakdown.get("total_texts", q.n_answered)
        samples = (breakdown.get("sample") or [])[:12]
        items = "".join(
            f'<li class="quote"><p>«&nbsp;{_esc(s)}&nbsp;»</p></li>'
            for s in samples
            if s
        )
        sample_block = (
            f'<figcaption>{L["open_text_sample"]}</figcaption><ul class="samples">{items}</ul>'
            if items
            else ""
        )
        return (
            f'<figure class="q-block avoid-break">{title}{takeaway_html}'
            f'<div class="chart-meta num">{L["open_text_total"].format(n=total)}</div>'
            f"{sample_block}</figure>"
        )

    if not body:
        return (
            f'<figure class="q-block avoid-break">{title}'
            f'<figcaption>{L["no_answers"]}</figcaption></figure>'
        )
    caption_html = f"<figcaption>{_esc(caption)}</figcaption>" if caption else ""
    return (
        f'<figure class="q-block avoid-break">{title}{takeaway_html}'
        f'<div class="chart-meta num">{_esc(" · ".join(meta_bits))}</div>'
        f'<div class="chart-wrap">{body}</div>{caption_html}</figure>'
    )


def render_survey_dashboard_html(dashboard, company_name: str = "", lang: str = "en") -> str:
    """Standalone print-ready HTML for a survey's quantitative results.

    ``dashboard`` — a SurveyDashboardPayload (services/survey_analytics).
    ``lang``      — the researcher's UI language (en/fr), normalised here.

    Everything rendered comes straight from the dashboard aggregates; the
    renderer never computes a proportion itself and never surfaces a
    percentage the stats layer suppressed below n=30.
    """
    lang = "fr" if (lang or "en").lower().startswith("fr") else "en"
    L = _SURVEY_STRINGS[lang]

    n_completed = dashboard.n_completed
    n_started = dashboard.n_started
    min_n = dashboard.min_n_threshold
    cr = dashboard.completion_rate_percentage
    completion_txt = f"{round(cr)}%" if cr is not None else L["below_threshold"].format(n=min_n)

    def _parse(iso: str | None) -> datetime | None:
        if not iso:
            return None
        try:
            return datetime.fromisoformat(iso)
        except (TypeError, ValueError):
            return None

    exported = _fmt_date(datetime.utcnow(), lang)
    start = _parse(dashboard.fielding_started_at)
    end = _parse(dashboard.fielding_ended_at)
    fielding_txt = ""
    if start:
        fielding_txt = _fmt_date(start, lang)
        if end:
            fielding_txt += f" – {_fmt_date(end, lang)}"

    meta_spans = [
        f'<span><strong>{n_completed}</strong> {L["responses"]}</span>',
        f'<span><strong>{n_started}</strong> {L["started_meta"]}</span>',
        f'<span><strong>{_esc(completion_txt)}</strong> {L["completion_meta"]}</span>',
        f'<span>{L["status_label"]}: {_esc(str(dashboard.status).upper())}</span>',
    ]
    if fielding_txt:
        meta_spans.append(f'<span>{L["fielding"]}: {_esc(fielding_txt)}</span>')
    meta_spans.append(f'<span>{L["exported"]} {exported}</span>')

    header = f"""
    <header class="cover">
      <div class="cover__brand">
        <span class="cover__logo">QualiPulse</span>
        <span class="cover__doctype">{L["doc_type"]}</span>
      </div>
      <h1 class="cover__title">{_esc(dashboard.name)}</h1>
      <div class="cover__meta num">{"".join(meta_spans)}</div>
    </header>
    """

    note = (
        L["note_below_threshold"].format(n=min_n)
        if n_started < min_n
        else L["note_wilson"].format(n=min_n)
    )
    stat_band = f"""
    <div class="stat-band">
      <div class="stat"><div class="stat__value num">{n_completed}</div><div class="stat__label">{L["stat_completed"]}</div></div>
      <div class="stat"><div class="stat__value num">{n_started}</div><div class="stat__label">{L["stat_started"]}</div></div>
      <div class="stat"><div class="stat__value num">{_esc(completion_txt)}</div><div class="stat__label">{L["stat_completion"]}</div></div>
      <div class="stat"><div class="stat__value num">{len(dashboard.questions)}</div><div class="stat__label">{L["stat_questions"]}</div></div>
    </div>
    """
    glance = f"""
    <section class="avoid-break">
      <div class="sec-head"><span class="sec-num num">1</span><span class="sec-title">{L["sec_glance"]}</span></div>
      {stat_band}
      <p class="method-note">{_esc(note)}</p>
    </section>
    """

    figures = "".join(
        _survey_question_figure(q, L, lang, _accent("quant")) for q in dashboard.questions
    )
    questions_section = f"""
    <section>
      <div class="sec-head"><span class="sec-num num">2</span><span class="sec-title">{L["sec_questions"]}</span></div>
      {figures}
    </section>
    """

    contract_items = "".join(f"<li>{_esc(item)}</li>" for item in L["contract_items"])
    methodology = f"""
    <section class="avoid-break">
      <div class="sec-head"><span class="sec-num num">3</span><span class="sec-title">{L["sec_methodology"]}</span></div>
      <div class="contract"><h4>{L["contract_title"]}</h4><ul>{contract_items}</ul></div>
    </section>
    """

    title = f"{dashboard.name} — {L['doc_type']}"
    return f"""<!doctype html>
<html lang="{lang}">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta name="robots" content="noindex">
<title>{_esc(title)}</title>
<style>{_SURVEY_CSS + _palette_css("quant")}</style>
</head>
<body>
<div class="toolbar"><button onclick="window.print()">{L["print_btn"]}</button></div>
<div class="sheet">
{header}
{glance}
{questions_section}
{methodology}
<footer class="doc-footer">
  <span><strong>QualiPulse</strong> · {_esc(company_name)}</span>
  <span>{L["footer"]}</span>
</footer>
</div>
</body>
</html>"""
